# Current Feature

## Status

<!-- Not Started | In Progress | Complete -->

## Goals

<!-- Bullet points of what success looks like -->

## Notes

<!-- Additional context, constraints, or details from spec -->

## History

<!-- Keep this updated. Earliest to latest -->

- **2026-06-06** — Initial Next.js 16 + React 19 + Tailwind v4 + TypeScript scaffold (Create Next App). Removed default boilerplate SVGs, edited starter `page.tsx` / `globals.css`, and added project context docs. Committed as `chore: initial next.js and tailwind setup` and pushed to `origin/main` (https://github.com/CanerOren/devstash.git). (Completed)
- **2026-06-08** — Dashboard UI Phase 1 (branch `feature/dashboard-phase-1`). Initialized shadcn/ui (Next template, radix base) — added `components.json`, `src/lib/utils.ts`, theme tokens in `globals.css`, and `ui/button.tsx` + `ui/input.tsx`. Enabled dark mode by default (`dark` class on `<html>`) and updated app metadata. Built the `/dashboard` shell: `dashboard/layout.tsx` (TopBar + Sidebar + main), `dashboard/page.tsx` (`Main` placeholder), `TopBar.tsx` (display-only search + New Collection / New Item buttons), and `Sidebar.tsx` (`Sidebar` placeholder). `npm run build` and `npm run lint` pass. (Completed)
- **2026-06-08** — Dashboard UI Phase 2 — functional sidebar (branch `feature/dashboard-phase-2`). Replaced the Phase 1 `Sidebar` placeholder with a data-driven sidebar fed from `src/lib/mock-data.ts`. Added `sidebar-context.tsx` (client context tracking desktop `collapsed` rail state + mobile drawer state), `SidebarContent.tsx` (item types linking to `/items/[type]`, favorite + recent collection sections, bottom user/avatar area), and `type-icons.ts` (maps `ItemType.icon` names → Lucide components). The sidebar collapses to a narrow icon-only rail (icons stay visible/clickable with tooltips) via a toggle in its own header next to a "Navigation" label; renders as an overlay drawer on mobile (menu button in `TopBar`, `md:hidden`). Added a white "DS" logo box left of "DevStash" in `TopBar`. Wired up the Geist font site-wide by fixing the broken self-referential `--font-sans` in `globals.css` (was silently falling back to the system font). `npm run build` and `npm run lint` pass. (Completed)
- **2026-06-08** — Dashboard UI Phase 3 — main content area (branch `feature/dashboard-phase-3`). Filled the `Main` placeholder in `dashboard/page.tsx` with the dashboard home: a header, 4 stats cards (total items, collections, favorite items, favorite collections — not in the screenshot), a Collections grid, a Pinned items list, and a Recent items list (sorted by `createdAt`, limit 10). All fed from `src/lib/mock-data.ts`. Added presentational server components `StatCard.tsx`, `CollectionCard.tsx` (top border tinted by primary item-type color, footer type icons), and `ItemCard.tsx` (left border in item-type color, tags, date). The page resolves item-type ids via a `Map` and passes resolved types down as props, keeping everything server-rendered (`createElement(getTypeIcon(...))` in `ItemCard` to satisfy the `react-hooks/static-components` lint rule). `npm run build`, `npm run lint`, and `npx tsc --noEmit` pass. (Completed)
- **2026-06-09** — Prisma 7 + Neon PostgreSQL setup (branch `feature/prisma-neon-setup`). Installed Prisma 7.8 with its new architecture: generator `provider = "prisma-client"` emitting the client **outside** `node_modules` to `src/generated/prisma`; datasource URL moved out of the schema into `prisma.config.ts` (which loads `dotenv/config` since the v7 CLI no longer auto-reads `.env`); a **driver adapter** (`@prisma/adapter-neon` `PrismaNeon` + `@neondatabase/serverless` over WebSockets via `ws`) is now required. Wrote the full initial schema (`User`, `Account`, `Session`, `VerificationToken`, `ItemType`, `Item`, `Collection`, `ItemCollection`, `Tag`, `ItemTag` + `ContentType` enum) with 13 indexes and `onDelete: Cascade` on all relations except `Item → ItemType` (RESTRICT). Split Neon connections: `DATABASE_URL` (pooled, app runtime) vs `DIRECT_URL` (unpooled, migrations). Added `src/lib/prisma.ts` (hot-reload-safe singleton wired to the Neon adapter), `prisma/seed.ts` (idempotent system `ItemType` seed via `findFirst` + create/update, since NULL `userId` defeats `upsert` on the `(name, userId)` unique index), and `scripts/test-db.ts` (standalone connection + row-count + seed-count smoke test). Added `db:*` npm scripts (`migrate`, `deploy`, `seed`, `studio`, `generate`, `test`) and a `postinstall: prisma generate`. Ran the initial migration `20260608142045_init` against the Neon **dev** branch (via `prisma migrate dev`, never `db push`) and seeded the 7 system types. `.env.example` template committed; generated client gitignored and ESLint-ignored. `npm run build`, `npm run lint`, `npx tsc --noEmit`, and `npm run db:test` all pass (test confirms 7 seeded types, all models queryable). (Completed)
- **2026-06-09** — Seed data (branch `feature/seed-data`). Rewrote `prisma/seed.ts` to seed sample dev/demo data on top of the existing system-type seed (spec in `context/features/seed-spec.md`). Added a **demo user** (`demo@devstash.io` / "Demo User", password `12345678` hashed with bcryptjs 12 rounds, `isPro: false`, `emailVerified: now`) upserted by unique email. Then seeds **5 collections / 18 items / 22 tags** all owned by that user: React Patterns (3 TS snippets — useDebounce, typed Context provider, `cn()` util), AI Workflows (3 prompts — code review, doc generation, refactoring), DevOps (1 Dockerfile snippet, 1 deploy command, 2 links), Terminal Commands (4 git/docker/process/npm commands), Design Resources (4 links — Tailwind, shadcn/ui, Radix, Lucide). `contentType` derived per type (TEXT for snippet/prompt/command/note, URL for link), `language` set on code snippets, relations wired via the `ItemCollection` / `ItemTag` join tables, tags deduped through a cache. **Idempotent**: upserts the user, then `deleteMany`s the user's items/collections/tags (cascades clear join rows) and rebuilds — re-running yields identical counts. Installed `bcryptjs` + `@types/bcryptjs`. Note: the dashboard still reads from `src/lib/mock-data.ts`, so seeded rows aren't shown in the UI yet (verified via seed output / Prisma Studio). `npm run db:seed` (run twice), `npx tsc --noEmit`, `npm run lint`, and `npm run build` all pass. (Completed)
- **2026-06-09** — Dashboard collections from DB (branch `feature/dashboard-collections`). Replaced the dummy collection cards in the dashboard main area with live data from Neon via Prisma (spec in `context/features/dashboard-collections-spec.md`); items (Pinned/Recent) still read from `src/lib/mock-data.ts`. Added `src/lib/db/collections.ts` with two server-side fetchers scoped to the seeded demo user (`demo@devstash.io`, no auth yet): `getDashboardCollections(limit = 6)` — returns each collection (most-recent first) with its `itemCount`, the **distinct item types** it contains ordered by frequency, and a `primaryColor` (the most-common type's color, for the card's border tint), computed from a single `findMany` that includes `items → item → itemType` (no N+1); and `getCollectionStats()` — `total` + `favorites` counts via two `prisma.collection.count` calls. Exposed view-model types `DashboardCollection` / `CollectionCardType` (the latter derives a display `label` by capitalizing the raw type name). Refactored `CollectionCard.tsx` to take a single `collection: DashboardCollection` prop (types + primaryColor now travel inside it) instead of the old mock `Collection`/`ItemType[]`/`primaryColor` trio. Updated `dashboard/page.tsx` to an `async` server component that `Promise.all`s both fetchers, feeds the Collections grid, and wires the **Collections** + **Favorite Collections** stat cards to real counts (Items/Favorite Items stats still mock). Added `export const dynamic = "force-dynamic"` so the page server-renders per request instead of prerendering stale data at build time (`/dashboard` now shows as `ƒ Dynamic` in the build output). `npx tsc --noEmit`, `npm run lint`, and `npm run build` all pass. (Completed)
- **2026-06-09** — Dashboard items from DB (branch `feature/dashboard-items`). Replaced the dummy Pinned/Recent item lists in the dashboard main area with live data from Neon via Prisma (spec in `context/features/dashboard-items-spec.md`); the dashboard now reads **no** item data from `src/lib/mock-data.ts` (only the sidebar still does). Added `src/lib/db/items.ts` with three server-side fetchers scoped to the seeded demo user (`demo@devstash.io`, no auth yet), all sharing one `itemInclude` (item type + tag names, no N+1) and a `toDashboardItem` mapper: `getPinnedItems()` — pinned items, most-recent first (empty array when none, so the Pinned section is omitted); `getRecentItems(limit = 10)` — most recently created items; and `getItemStats()` — `total` + `favorites` counts via two `prisma.item.count` calls. Exposed view-model types `DashboardItem` (bundles a resolved `type` + flattened `tags: string[]`) and `DashboardItemType` (derives a display `label` by capitalizing the raw type name). Refactored `ItemCard.tsx` to take a single `item: DashboardItem` prop (type now travels inside it) instead of the old mock `Item`/`ItemType` pair, and `formatDate` now accepts a `Date` rather than an ISO string. Updated `dashboard/page.tsx` to `Promise.all` all five fetchers (2 collection + 3 item), dropped the `mock-data` import and the `typeById` lookup map, and wired the **Items** + **Favorite Items** stat cards to real counts (all four stat cards now DB-backed). Note: no seeded item has `isPinned: true`, so the Pinned section currently renders nothing (intended per spec). `npx tsc --noEmit`, `npm run lint`, and `npm run build` all pass. (Completed)
- **2026-06-10** — Stats & sidebar from DB (branch `feature/stats-sidebar`). Wired the dashboard **sidebar** to live Neon/Prisma data so it no longer reads `src/lib/mock-data.ts` (the main-area stats were already DB-backed; this closed out the last mock-fed surface). Added three server-side fetchers scoped to the demo user (`demo@devstash.io`, no auth yet): `getSidebarItemTypes()` in `src/lib/db/items.ts` — the 7 **system** item types in a canonical `TYPE_ORDER` (snippet→prompt→command→note→file→image→link, since `ItemType` has no timestamp to sort on), each with the user's live item count from a single `prisma.item.groupBy({ by: ["itemTypeId"], _count: true })`, a plural display `label`, and an `href` (`/items/snippets`, …); `getSidebarCollections()` in `src/lib/db/collections.ts` — all of the user's collections (most-recent first) with `itemCount`, `isFavorite`, and a `primaryColor` (most-common type's color) computed from one `findMany` (no N+1); and a new `src/lib/db/user.ts` → `getCurrentUser()` returning the demo user's `name`/`email`/`image` via `findUniqueOrThrow`. Exposed view-model types `SidebarItemType`, `SidebarCollection`, `SidebarUser`. Since `SidebarContent.tsx` is a client component, the data is fetched in `dashboard/layout.tsx` (now an `async` server component, `export const dynamic = "force-dynamic"`, `Promise.all`s all three fetchers) and threaded down through `Sidebar.tsx` as props (replacing the `collections`/`itemTypes`/`currentUser` mock imports). Sidebar UX: favorites keep the amber **star**; recent collections now show a **colored circle** tinted by their most-used type; added a **"View all collections"** link (→ `/collections`) under the list in both the expanded view and the icon rail; the user area shows the DB user (name falls back to email for display + avatar initials via a new `initialsOf` helper, since `User.name` is nullable). Also fixed the dashboard shell to **pin to the viewport** (`dashboard/layout.tsx` outer `min-h-screen` → `h-screen` + `overflow-hidden`) so the TopBar and sidebar stay fixed and only `<main>` scrolls. Seed: added an optional `isFavorite` to `SeedCollection` and marked **React Patterns** + **Terminal Commands** as favorites (the seed previously set none, so the sidebar Favorites section was empty), then re-ran `npm run db:seed` (still idempotent — 5 collections / 18 items / 22 tags). `src/lib/mock-data.ts` is now unused by all source but was left in place. `npx tsc --noEmit`, `npm run lint`, and `npm run build` all pass (`/dashboard` stays `ƒ Dynamic`). (Completed)
- **2026-06-11** — Pro badge in sidebar (branch `feature/add-pro-badge-sidebar`, spec in `context/features/add-pro-badge-sidebar.md`). Added a subtle **"PRO"** badge next to the **Files** and **Images** item types in the dashboard sidebar (the two Pro-only types per the project overview's type table). Installed the shadcn/ui **Badge** component (`npx shadcn add badge` → `src/components/ui/badge.tsx`, a `cva`-based variant `<span>`). In `SidebarContent.tsx`'s `typeRow`, derived `const isPro = type.name === "file" || type.name === "image"` and rendered `<Badge variant="secondary">PRO</Badge>` between the type label and the count, with compact classes (`h-4 px-1.5 text-[0.6rem] font-semibold tracking-wider text-muted-foreground`) to keep it small and muted. The badge lives inside the existing `{!isRail && …}` block, so it shows only in the expanded sidebar — never in the collapsed icon rail. Purely presentational: no data-layer change, since `SidebarItemType.name` already carried the raw type name. `npx tsc --noEmit`, `npm run lint`, and `npm run build` all pass. (Completed)
- **2026-06-11** — DB fetcher quick wins (branch `feature/db-fetcher-quick-wins`). Low-risk cleanups to the `src/lib/db/*` fetchers surfaced by a code-scanner audit. **Capped two unbounded queries**: `getPinnedItems()` (`items.ts`) and `getSidebarCollections()` (`collections.ts`) each gained `take: 20` (the sidebar one previously loaded ALL of a user's collections — with nested `items → itemType` — on every navigation; the pinned one ran unbounded on every dashboard render). **Removed an unsafe type cast** in `getSidebarCollections`: `colorById.get(primaryId) as string` → `colorById.get(primaryId) ?? "var(--muted-foreground)"` (type-safe, behavior-identical, no `any`-style escape per coding-standards). **De-duplicated two shared values** into a new `src/lib/db/helpers.ts`: `DEMO_USER_EMAIL` (was copy-pasted in `items.ts`, `collections.ts`, `user.ts` — now a single auth seam for the future NextAuth swap) and the `toLabel()` capitalizer (was identical in `items.ts` + `collections.ts`), both imported by the consuming modules. No schema/index/migration changes (verified the dev/prod Neon branches stay in sync), no UI change. Two behavioral changes only — the `take: 20` caps, both safe limits well above current seeded data (5 collections / 0 pinned items). Out-of-scope audit items (decomposing `SidebarContent.tsx`, moving the seed password to env, `test-db.ts` over-fetch) intentionally left for later. `npx tsc --noEmit`, `npm run lint`, and `npm run build` all pass. (Completed)
- **2026-06-15** — Auth Phase 1: NextAuth v5 + GitHub OAuth (branch `feature/auth-phase-1`, spec in `context/features/auth-phase-1-spec.md`). Installed `next-auth@5.0.0-beta` + `@auth/prisma-adapter` and wired up Auth.js with the **split-config pattern** for edge compatibility. `src/auth.config.ts` holds the edge-safe providers-only config (GitHub, reading `AUTH_GITHUB_ID`/`AUTH_GITHUB_SECRET` from env, typed `satisfies NextAuthConfig`); `src/auth.ts` is the full Node config that spreads it and adds `PrismaAdapter(prisma)` + `session: { strategy: "jwt" }`, with `jwt`/`session` callbacks threading `user.id` onto the session. `src/app/api/auth/[...nextauth]/route.ts` re-exports the `GET`/`POST` handlers; `src/proxy.ts` (Next.js 16 middleware, `export const proxy = auth(...)`, `matcher: ["/dashboard/:path*"]`) initializes NextAuth with only the edge-safe config and redirects unauthenticated `/dashboard/*` requests to NextAuth's default sign-in page (`/api/auth/signin`); `src/types/next-auth.d.ts` augments `Session.user`/`JWT` with `id`. Also added MCP/agent tooling (`.mcp.json`, `.claude/agents/code-scanner.md` + agent-memory), a Neon MCP usage section in `CLAUDE.md` (always target the `development` branch, never touch `production`), and gitignored `logs/` + `.playwright-mcp/`. No unit tests — the feature is config/wiring with no standalone testable logic and no test runner is configured (verify via browser: `/dashboard` → sign-in → GitHub → back to `/dashboard`). `npm run build` passes (`/api/auth/[...nextauth]` route + Proxy middleware both register). Left out of this commit for later handling: the phase-2/3 specs. (Completed)
- **2026-06-15** — Auth Phase 2: Credentials (email/password) provider + registration (branch `feature/auth-phase-2`, spec in `context/features/auth-phase-2-spec .md`). Added email/password sign-in on top of the Phase 1 GitHub OAuth setup, keeping the **split-config pattern**. `src/auth.config.ts` now declares a **Credentials placeholder** alongside GitHub — it imports the edge-safe `next-auth/providers/credentials` (no bcrypt/Prisma), defines the `email`/`password` field shape so the default `/api/auth/signin` form renders, and uses `authorize: () => null`. `src/auth.ts` builds the **real** Credentials provider (bcrypt + Prisma): it looks up the user by lower-cased email via `prisma.user.findUnique`, rejects OAuth-only users (`user.password` is null), and `bcrypt.compare`s the password, returning `{ id, name, email, image }` on success. The override is wired by mapping over `authConfig.providers` and swapping the entry whose `provider.id === "credentials"` (GitHub is a function reference, so `typeof !== "function"` skips it), so it replaces only Credentials and leaves GitHub untouched; `providers` is set after the `...authConfig` spread so it wins. Added the registration API route `POST /api/auth/register` (`src/app/api/auth/register/route.ts`): a Zod `registerSchema` validates `name`/`email`/`password`/`confirmPassword` (trim, lower-case email, 8–72 char password) with a `.refine` enforcing passwords match; the handler rejects duplicate emails (409), `bcrypt.hash(password, 12)` (matching the seed's cost), creates the user with `select: { id, name, email }` (never returns the hash), and returns the project's `{ success, data?, error? }` shape (201 on success, 400 invalid, 409 duplicate, 500 on error). `User.password` already existed in the schema (`String?`, nullable for OAuth-only) so **no migration** was needed. Installed `zod` (was missing despite coding-standards requiring it for input validation). Verified via build/lint/tsc; runtime curl + sign-in testing handed to the user. `npx tsc --noEmit`, `npm run lint`, and `npm run build` all pass (`/api/auth/register` registers as a route). (Completed)
- **2026-06-15** — Auth Phase 3: Custom auth UI + session-scoped dashboard (branch `feature/auth-phase-3`, spec in `context/features/auth-phase-3-spec.md`). Replaced the default NextAuth pages with custom UI and switched the dashboard from the hardcoded demo user to the **authenticated session user**. **Pages** live under a new `src/app/(auth)/` **route group** (parentheses keep URLs at `/sign-in` + `/register`) sharing `(auth)/layout.tsx` (centered shell + DS logo). `SignInForm.tsx` (client): email/password via `signIn("credentials", { redirect: false })`, "Sign in with GitHub" via `signIn("github")` (inline `GitHubIcon.tsx` SVG since lucide dropped brand icons), link to register, honors `callbackUrl`, and shows both credentials errors (inline `formError`) and OAuth failures (`?error=` search param) plus a post-register success banner (`?registered=1`). `RegisterForm.tsx` (client): name/email/password/confirm, client passwords-match guard + `type="email"`/`minLength=8`, POSTs to the existing `/api/auth/register`, redirects to `/sign-in?registered=1`. Pages stay server components wrapping the client forms (sign-in wraps `SignInForm` in `Suspense` since it reads `useSearchParams`). **Routing**: added `pages: { signIn: "/sign-in" }` to `auth.config.ts`; `proxy.ts` now redirects unauthenticated `/dashboard/*` to `/sign-in?callbackUrl=…` (was the default `/api/auth/signin`). **Avatar/sidebar**: new reusable `src/components/user/UserAvatar.tsx` (GitHub `image` or `initialsOf` fallback, on the shadcn Avatar primitive); installed shadcn `avatar` + `dropdown-menu`. The sidebar user area (`SidebarContent.tsx`) now uses `UserAvatar` and opens an upward dropdown with **Profile** (→ `/profile`) and **Sign out** (`signOut({ callbackUrl: "/sign-in" })`); removed the old local `initialsOf` + Settings link. **Session scoping** (approved extension beyond the UI-only spec): replaced the `DEMO_USER_EMAIL` constant with `requireUserId()` in `src/lib/db/helpers.ts` (reads `auth()` → `session.user.id`, throws if absent, wrapped in React `cache()` so the JWT decode runs once per request); rescoped every fetcher in `items.ts`, `collections.ts`, `user.ts` to the session `userId` (direct column filter, not the `{ user: { email } }` relation). `getCurrentUser()` now loads the session user fresh from the DB. Net effect: signing in as `demo@devstash.io` / `12345678` shows the seeded data; a brand-new GitHub/registered account gets an empty dashboard (intended). Also updated the `code-scanner` / `nextjs-codebase-auditor` agent-memory notes that said "no auth yet / DEMO_USER_EMAIL intentional" to reflect that auth is now wired. **Known gap**: the `/profile` page isn't built yet (the dropdown links to it per spec — 404 until a future feature). The pre-existing unrelated `next.config.ts` working-tree change (`devIndicators: false`) was deliberately left **out** of the commit. No migration (no schema change). No unit tests (forms/config wiring; no test runner configured) — verify in browser per the spec's testing checklist. `npx tsc --noEmit`, `npm run lint`, and `npm run build` all pass. (Completed)
- **2026-06-16** — Email verification on register (branch `feature/email-verification`). Send a verification email via **Resend** on email/password registration; credentials sign-in is now blocked until the address is verified (GitHub OAuth users are unaffected — they never hit the Credentials provider). Installed `resend@6`. **Email wrapper** `src/lib/email.ts`: a `Resend` client + `sendVerificationEmail({ to, name, url })` (text + HTML body) that never throws — returns a boolean; sender defaults to `DevStash <onboarding@resend.dev>` (Resend's shared dev sender, which in test mode only delivers to the Resend account owner's own email), overridable via a new optional `EMAIL_FROM` env var. **Token helpers** `src/lib/auth/verification.ts`: reuse the existing NextAuth `VerificationToken` table (so **no migration**) — `createVerificationToken(email)` deletes any prior tokens for that identifier then creates a 32-byte hex token with a 24h expiry; `buildVerificationUrl(origin, token)`; and `issueVerificationEmail({ origin, email, name })` (shared by register + resend) which creates the token, builds the link from the request origin (`new URL(request.url).origin` — no hardcoded base URL), and sends. **Register route** (`/api/auth/register`): after `prisma.user.create`, calls `issueVerificationEmail`; per decision the account is **still created (201)** even if the email fails to send, returning an `emailSent` flag so the user can resend. **New routes**: `GET /api/auth/verify-email?token=…` validates + **consumes** (deletes) the token, sets `User.emailVerified` via `updateMany(where emailVerified: null)` (idempotent), and redirects to `/sign-in?verified=1` (or `?verifyError=expired|invalid`); `POST /api/auth/resend-verification` (Zod `{ email }`) re-issues the link but returns a **generic success** regardless of whether the account exists/is verified (anti-enumeration), only actually sending for an unverified credentials user. **Sign-in gate** `src/auth.ts`: the Credentials `authorize` now throws an `EmailNotVerifiedError extends CredentialsSignin` (`code = "unverified"`) when the password is valid but `emailVerified` is null. **`SignInForm.tsx`**: reads `result.code === "unverified"` from `signIn(..., { redirect: false })` to show a "verify your email" error with a **Resend verification email** button (POSTs the entered email to the resend route), plus new banners for `?verified=1` (success), `?verifyError=expired|invalid`, and reworded the post-register banner to say "check your email". `.env.example` documents `RESEND_API_KEY` + `EMAIL_FROM` (note: `.env*` is currently gitignored, so that edit stays local). Decisions taken with the user: sender = `onboarding@resend.dev`; email-send failure = create account + offer resend. The pre-existing unrelated `next.config.ts` change (`devIndicators: false`) was again left **out** of the commit. Also added (separate `chore:` commit) `scripts/prune-users.ts` — a tsx utility that deletes every user except `demo@devstash.io` and their cascaded content (+ their `VerificationToken` rows), printing the target DB host and aborting if the demo user is missing; run several times against the Neon **development** branch during testing. No unit tests (no runner configured) — verified via browser + the prune script's token output. `npx tsc --noEmit`, `npm run lint`, and `npm run build` all pass (both new routes register). (Completed)
- **2026-06-17** — Toggle for email verification (branch `feature/email-verification-toggle`). Added a single env-driven switch to enable/disable the entire email-verification system, read everywhere through one helper so all touchpoints stay in sync. **Helper** `src/lib/auth/verification.ts`: new `isEmailVerificationEnabled()` — returns `process.env.EMAIL_VERIFICATION_ENABLED !== "false"` (enabled by **default**; set the env var to `"false"` to turn off, e.g. while Resend has no verified sending domain so non-owner addresses can't receive mail). **Register route** (`/api/auth/register`): when verification is **off**, the user is created with `emailVerified: new Date()` (so the sign-in gate passes) and **no** verification email is sent; the response now also carries `verificationRequired`. When on, behavior is unchanged. **Sign-in gate** `src/auth.ts`: the Credentials `authorize` only throws `EmailNotVerifiedError` when `isEmailVerificationEnabled() && !user.emailVerified` — so the gate is bypassed when off (GitHub OAuth users never hit this provider, unaffected either way). **Resend route** (`/api/auth/resend-verification`): short-circuits to the same generic anti-enumeration success when off (nothing to resend), indistinguishable from the enabled path. **UI**: `RegisterForm.tsx` reads `verificationRequired` from the register response and appends `&verify=0` to the `/sign-in?registered=1` redirect when off; `SignInForm.tsx` reads `?verify=0` and shows "Account created. You can now sign in." instead of the "check your email" banner. Semantics confirmed with the user: an account created while off stays verified if the flag is later turned on (acceptable). **No DB schema/migration change** (reuses `User.emailVerified` + the existing `VerificationToken` table). Also enabled `next.config.ts` `devIndicators: false` (the pre-existing working-tree change that prior features had deliberately left out — folded in this time). `.env.example` documents `EMAIL_VERIFICATION_ENABLED` (note: `.env*` is gitignored, so that edit stays local). No unit tests (no runner configured) — verified via build + browser. `npx tsc --noEmit`, `npm run lint`, and `npm run build` all pass. (Completed)
- **2026-06-17** — Forgot password (branch `feature/forgot-password`). Let email/password users reset a forgotten password via a time-limited reset link, **reusing the `VerificationToken` table** (no migration). **Token helpers** `src/lib/auth/password-reset.ts` mirror `verification.ts` (`randomBytes(32).toString("hex")`, delete-then-create) but with a **1-hour TTL** and reset tokens **namespaced** by a `reset:` identifier prefix (`resetIdentifier(email)` → `reset:<lowercased-email>`; `emailFromResetIdentifier` recovers the email or returns null for non-reset tokens). The namespace keeps the two flows isolated: issuing a reset never wipes a pending verification token, a verification token POSTed to `/reset-password` is rejected (null email) without being consumed, and a reset token sent to `/verify-email` verifies nobody (`updateMany` matches no user). Exposes `createPasswordResetToken`, `buildPasswordResetUrl` (→ the `/reset-password` **page**, not an API route, since the user needs a form), and `issuePasswordResetEmail`. **Email** `src/lib/email.ts`: added `sendPasswordResetEmail` (+ HTML template) — same Resend client, `FROM`/`EMAIL_FROM` default, never-throws-returns-boolean contract as `sendVerificationEmail`. **Routes**: `POST /api/auth/forgot-password` (Zod `{ email }`) issues a token + emails the link, always returns a **generic success** (anti-enumeration), and only sends for a credentials user (`user?.password`) — OAuth-only/non-existent users silently no-op; `POST /api/auth/reset-password` (Zod `{ token, password, confirmPassword }`, 8–72 chars + confirm-match `.refine`, matching register) validates the token (exists, is a reset token, not expired), then in a `prisma.$transaction` sets the new `bcrypt.hash(password, 12)` **and** consumes (deletes) the token, also setting `emailVerified` if it was null (a completed reset proves email ownership). Invalid/expired/OAuth-only cases return a stable `code` (`invalid`|`expired`) and consume the token where appropriate. **UI** (under `src/app/(auth)/`): new `/forgot-password` page + `ForgotPasswordForm` (email → generic "check your inbox" confirmation, anti-enumeration) and `/reset-password?token=…` page + `ResetPasswordForm` (Suspense-wrapped for `useSearchParams`, handles a missing/blank token, redirects to `/sign-in?reset=1` on success). `SignInForm.tsx` gained a **"Forgot password?"** link beside the password field and a `?reset=1` success banner (and now excludes `justReset` from the post-register banner condition). **No DB schema/migration change.** GitHub OAuth-only users are unaffected. Decisions taken at start: reset TTL = 1 hour; a successful reset also sets `emailVerified` if null. Known limitations (consistent with existing auth routes, out of scope): no rate limiting and an observable response-timing difference between existing vs. non-existent accounts — same as `resend-verification`. No new env var (reuses `RESEND_API_KEY`/`EMAIL_FROM`). No unit tests (no runner configured) — verified via build; runtime browser test deferred (Resend test mode only delivers to the account owner). `npx tsc --noEmit`, `npm run lint`, and `npm run build` all pass (both new routes + both new pages register). (Completed)
- **2026-06-17** — Profile page (branch `feature/profile`, spec in `context/features/profile-spec.md`). Built the protected `/profile` page (fills the known 404 gap from Auth Phase 3 — the sidebar dropdown's Profile link). **App shell extraction**: pulled the dashboard shell (sidebar-data `Promise.all` + `SidebarProvider`/`TopBar`/`Sidebar`/scrollable `main`) out of `dashboard/layout.tsx` into a reusable server component `src/components/dashboard/AppShell.tsx`; `dashboard/layout.tsx` is now a thin wrapper around it, and a new `src/app/profile/layout.tsx` reuses the same `AppShell` (both keep `export const dynamic = "force-dynamic"`) so `/profile` renders **inside** the dashboard chrome (top bar + sidebar) while keeping its top-level URL. **Data**: `src/lib/db/user.ts` → new `getProfileUser()` (scoped via `requireUserId()`) returns `name`/`email`/`image`/`createdAt` + a derived `hasPassword` boolean (the bcrypt hash is destructured out, never exposed); reused `getItemStats()`, `getCollectionStats()`, and `getSidebarItemTypes()` (already the 7 system types in canonical order with per-user counts) for the stats + type breakdown — no new fetchers. **Server actions** `src/actions/profile.ts` (new `src/actions/` dir per coding-standards, `"use server"`, Zod, `{ success, error? }` shape): `changePassword` (8–72 + confirm `.refine` matching register/reset; `requireUserId`; rejects OAuth-only accounts; `bcrypt.compare` on current then `bcrypt.hash(…, 12)`) and `deleteAccount` (`$transaction` that clears leftover verification/reset tokens — keyed by email so they don't cascade, via `resetIdentifier(email)` — then `prisma.user.delete`, cascading items/collections/tags/sessions/accounts; client signs out after). **Page** `src/app/profile/page.tsx` (server component, `dynamic = "force-dynamic"`, page-level `auth()` redirect guard as defense-in-depth, `Promise.all` of the 4 fetchers): styled to the provided screenshots — **Account Information** card (`UserAvatar`, name, provider line derived from `hasPassword` → "Email & password account" vs "Signed in with GitHub", Email + Member-since rows with icons), **Usage Statistics** card (two stat tiles + an "Items by Type" 4-col grid with all 7 types via `createElement(getTypeIcon(...))` for the static-components lint rule), and an **Account Settings** card holding the two modal buttons. **Modals**: `ChangePasswordDialog.tsx` (shadcn **Dialog**, shown only when `hasPassword`) wraps `ChangePasswordForm.tsx` (inline success/error banners matching the auth forms); `DeleteAccountDialog.tsx` (shadcn **AlertDialog**) gates the destructive action behind a **type-`DELETE`-to-confirm** input — the red confirm button (`variant="destructive"`, not a className override, since `AlertDialogAction` forwards `variant` to `Button` and the override lost the merge battle across the `asChild` Slot) stays disabled until the exact word is typed, and the typed text + error reset on every open/close. **Auth**: `proxy.ts` matcher + gate extended from `/dashboard` only to a `PROTECTED_PREFIXES` list covering `/dashboard` and `/profile` (`matcher: ["/dashboard/:path*", "/profile/:path*"]`). Installed shadcn **alert-dialog** + **dialog**. Provider line uses the `hasPassword` heuristic (accurate for this app's signup paths; would need the `Account` table if both methods are ever linked to one user). **No DB schema/migration change.** No unit tests (forms/actions wiring; no runner configured) — verified via build; runtime browser testing handed to the user. `npx tsc --noEmit`, `npm run lint`, and `npm run build` all pass (`/profile` registers as `ƒ Dynamic`). (Completed)
- **2026-06-27** — Rate limiting for auth (branch `feature/rate-limiting`, spec in `context/features/rate-limiting-spec.md`). Added Upstash Redis sliding-window rate limiting to the auth endpoints to guard against brute force, credential stuffing, and email-send abuse. Installed `@upstash/ratelimit` + `@upstash/redis`. **Utility** `src/lib/rate-limit.ts`: a lazy `Redis` singleton built from `UPSTASH_REDIS_REST_URL`/`UPSTASH_REDIS_REST_TOKEN` (cached, incl. the "not configured" → null outcome), a per-name `Ratelimit` cache (`Ratelimit.slidingWindow`, `prefix: ratelimit:<name>`, `analytics: false`), a `RATE_LIMITS` config map (login 5/15m, register 3/1h, forgotPassword 3/1h, resetPassword 5/15m, resendVerification 3/15m) typed `satisfies Record<string, RateLimitConfig>`, `checkRateLimit(name, identifier, config?)` → `{ success, remaining, reset }`, `getClientIp(request)` (`x-forwarded-for` first hop → `x-real-ip` → `"unknown"`), and `tooManyRequestsResponse(reset)` (429 JSON `{ success:false, error }` + `Retry-After` header, wait rounded up to whole minutes). **Fails open**: a missing client or a thrown check both return `success: true` so rate limiting never blocks auth when Upstash is down. **Routes**: `register`, `forgot-password`, `reset-password` check at the top of the handler keyed by IP; `resend-verification` checks after parsing the email, keyed by `IP:email` (before the verification-disabled short-circuit, so the send endpoint is throttled either way; a 429 leaks nothing — anti-enumeration preserved). **Login**: the `/api/auth/callback/credentials` route is owned by NextAuth, so the check lives **inside the Credentials `authorize(credentials, request)` callback** (keyed `IP:email`, before the DB/bcrypt work), throwing a new `RateLimitedError extends CredentialsSignin` (`code = "rate_limited"`) — chosen over a custom sign-in handler. **UI**: `SignInForm.tsx` maps `result.code === "rate_limited"` to a "Too many sign-in attempts…" message; the four `fetch`-based forms (`RegisterForm`, `ForgotPasswordForm`, `ResetPasswordForm`, and the resend button) already render the 429 `data.error` body, so they needed **no changes**. Per existing project convention, errors surface via inline banners, not toasts (no toast lib installed — followed the existing pattern rather than introduce one). **Env**: `.env.example` documents the two `UPSTASH_*` vars as optional (unset ⇒ fail open); `.env` + `.env.production` already carried real values (the user pre-added them — both currently point at the same Upstash instance, acceptable since IP-keyed identifiers rarely collide across localhost vs prod). Known limitation (matches spec): login is keyed per `(IP, email)`, so 5 attempts are allowed *per account* — broad credential stuffing across many accounts from one IP isn't globally capped; a per-IP cap could be layered later. A rate-limiting middleware refactor remains a possible future cleanup (out of scope). No DB schema/migration change. No unit tests (no runner configured) — `npx tsc --noEmit`, `npm run lint`, and `npm run build` all pass (all five auth routes + Proxy middleware register). (Completed)
- **2026-06-27** — Fix GitHub OAuth redirect (branch `fix/github-auth-redirect`, spec in `context/fixes/github-auth-redirect-fix.md`). Fixed the long-standing **two-click GitHub sign-in**: the first click authenticated (session created) but the client-side `signIn` from `next-auth/react` failed to redirect, bouncing back to `/sign-in`; only a second click landed on `/dashboard`. Root cause is the unreliable client-side redirect of `next-auth/react`'s `signIn` in production. **Fix** (the recommended NextAuth v5 pattern): moved GitHub sign-in to a **server action**. New `src/actions/auth.ts` (`"use server"`) exports `signInWithGitHub`, which calls the server-side `signIn` from `@/auth` with `signIn("github", { redirectTo: "/dashboard" })` (v5 `redirectTo`, not v4 `callbackUrl`) — `signIn` throws a redirect, so NextAuth owns it server-side and avoids the client-side timing issue. **`SignInForm.tsx`**: the GitHub button's inline `onClick={() => signIn("github", { callbackUrl })}` was replaced with a `<form action={signInWithGitHub}>` wrapping a `type="submit"` button; the `next-auth/react` `signIn` import stays (still used by the credentials `onSubmit`, which is **unchanged** — `redirect: false` + `router.push`). The spec's "remove `isGitHubLoading` / `handleGitHubSignIn`" step didn't apply (this file used an inline `onClick`, not that state/function). Note: the GitHub path now always redirects to a fixed `/dashboard` (no longer honors `?callbackUrl`); credentials still honors `callbackUrl`. No `SessionProvider` needed. **No DB schema/migration change.** The root-cause fix is a server-side redirect, so the real proof is first-click behavior in production (build can't exercise it) — runtime verification handed to the user. `npx tsc --noEmit`, `npm run lint`, and `npm run build` all pass (`/sign-in` stays statically prerendered). Done as part of the same session: a separate `chore:` (branch `chore/setup-vitest`) that stood up the **Vitest** test runner (native `@/*` alias resolution, `test`/`test:watch` scripts) with a first real suite for the password-reset token-namespace helpers (5 tests, prisma/email mocked) — committed and merged ahead of this fix. (Completed)
- **2026-06-29** — Items list view (branch `feature/item-list-view`, spec in `context/features/item-list-view-spec.md`). Built the dynamic `/items/[type]` route that lists the current user's items of one system type — the missing target for the sidebar's item-type links (`getSidebarItemTypes()` already emits `/items/${name}s` hrefs). **Fetcher** `src/lib/db/items.ts` → new `getItemsByType(typeSlug)` (scoped via `requireUserId()`, returns `ItemsByType | null`): loads the 7 **system** item types and matches the plural URL slug against the same `${type.name}s` pluralization the sidebar uses (so the slug round-trips exactly rather than guessing the singular), returns **null** for an unknown slug so the page can 404, then fetches that type's items (`where: { userId, itemTypeId }`, `orderBy createdAt desc`, **`take: 100`**) through the shared `itemInclude` + `toDashboardItem` mapper. Exposed view-model interface `ItemsByType` (`{ type: DashboardItemType; items: DashboardItem[] }`). **Route** `src/app/items/[type]/page.tsx` (server component, `export const dynamic = "force-dynamic"`, `params` is a `Promise` per Next 16): awaits `getItemsByType`, calls `notFound()` on null, renders a header (type-tinted icon chip via `createElement(getTypeIcon(...))` for the static-components lint rule, plural label, singular/plural item count) and a responsive `grid gap-3 md:grid-cols-2` of the existing **`ItemCard`** (left border in item-type color — reused unchanged), with a dashed empty-state card when the user has none. **Layout** `src/app/items/layout.tsx` reuses the shared `AppShell` (sidebar + top bar, `force-dynamic`) so `/items/*` renders inside the dashboard chrome. **Auth**: `proxy.ts` adds `/items` to both `PROTECTED_PREFIXES` and the `matcher` so the route is gated (otherwise `requireUserId()` throws). Pro-only types (`/items/files`, `/items/images`) render normally for free users, consistent with the sidebar already showing those rows (Pro gating remains a separate concern). **No DB schema/migration change.** No unit tests — `getItemsByType` is a Prisma fetcher with only a tiny pure seam (the slug↔type plural match), consistent with its untested sibling fetchers in `items.ts`; per the review this was left optional. Committed as two focused commits (`feat:` for the route + a separate `chore:` folding in the earlier Vitest-scope/doc-refinement changes that were sharing the working tree). `npx tsc --noEmit`, `npm run lint`, `npm test` (5 pass), and `npm run build` all pass (`/items/[type]` registers as `ƒ Dynamic`). (Completed)
- **2026-06-29** — Three-column item list on large screens (branch `feature/three-column-item-list`). Purely presentational tweak to the `/items/[type]` list view: the items grid in `src/app/items/[type]/page.tsx` went from `grid gap-3 md:grid-cols-2` to `grid gap-3 md:grid-cols-2 lg:grid-cols-3`, so item cards now flow **1 → 2 → 3** columns (mobile → md → lg) instead of capping at two. One-line change — no data-layer, schema, or component changes; `ItemCard` reused unchanged. The unrelated pre-existing `.claude/skills/list-components/SKILL.md` working-tree edit was deliberately left **out** of the commit. No unit tests (CSS-only change; no testable server-action/utility logic). `npm run build` passes (`/items/[type]` stays `ƒ Dynamic`). (Completed)
- **2026-06-29** — Item detail drawer (branch `feature/item-drawer`, spec in `context/features/item-drawer-spec.md`). Built a right-side slide-in **drawer** that doubles as the item detail view (no separate item page) — clicking an `ItemCard` opens it on **both** the dashboard and the `/items/[type]` list. Per the spec's **data-fetching split**, card data stays server-rendered (shown instantly so the header/description/tags don't flash) while **full detail is fetched on click** via a new API route. **Fetcher** `src/lib/db/items.ts` → `getItemDetail(id)` + `ItemDetail` interface: a user-scoped `prisma.item.findFirst({ where: { id, userId } })` (another user's id → `null` → 404) with one `include` (item type, tag names, collections) mapped to a flat view model (flattened `tags`/`collections`, capitalized type `label`). **API route** `src/app/api/items/[id]/route.ts` → `GET` with an explicit `auth()` check (401), calls the fetcher (404 if null, 500 on error), returns the project's `{ success, data?, error? }` shape; the route owns its own auth so it didn't need adding to `proxy.ts`. **Client wrapper** `src/components/items/item-drawer-context.tsx` → `ItemDrawerProvider` + `useItemDrawer()` hook owns open/summary/detail/loading/error state and the on-click `fetch`, with a **`currentId` ref** guarding against out-of-order responses (rapid clicks); mounted once in `AppShell` so it covers every signed-in route. **Drawer UI** `src/components/items/ItemDrawer.tsx` (shadcn **Sheet**, `side="right"`): header (type-tinted icon chip via `createElement(getTypeIcon(...))`, title, type + language **Badge**s), the **action bar** (Favorite/Pin/Copy/Edit grouped left, Delete right-aligned via `ml-auto`), and body sections (Description, Content, Tags, Collections, Details/dates) with **Skeleton** placeholders while detail loads. `ContentBlock` switches on `contentType` (code `<pre>` for TEXT, link for URL, file info for FILE). Exposes `ItemDetailResponse` (the wire type — `ItemDetail` with **string dates**, since JSON serializes `Date`). Per spec scope this is **display-only**: **Copy is functional** (clipboard); Favorite/Pin reflect state and Edit/Delete are present for layout but their mutations are deferred to a later feature. `ItemCard.tsx` became a `"use client"` clickable `<button>` calling `openItem(item)` (visuals unchanged). Drawer widened (overrode the Sheet's built-in `data-[side=right]:sm:max-w-sm` cap — its attribute selector wins on specificity — with the same modifier prefix: `sm:max-w-2xl`/`lg:max-w-4xl`/`xl:max-w-5xl`) and a visually-hidden `SheetDescription` added to satisfy Radix's `aria-describedby` warning; `aria-label` on the icon-only action buttons. Installed shadcn **sheet** + **skeleton**. **Unit tests** `src/lib/db/items.test.ts` (5 tests, prisma + helpers mocked via `vi.hoisted`): `getItemDetail` null/scoping/mapping and `getItemsByType` unknown-slug + plural-slug resolution + user scoping (10 total with the existing suite). Committed as a `feat:` (drawer + tests + spec) plus a separate `chore:` linking the TopBar **DevStash logo → `/dashboard`** (a `next/link` follow-up, unrelated to the drawer); the pre-existing unrelated `.claude/skills/list-components/SKILL.md` working-tree edit was again left **out**. **No DB schema/migration change.** `npx tsc --noEmit`, `npm run lint`, `npm test` (10 pass), and `npm run build` all pass (`/api/items/[id]` registers as `ƒ Dynamic`). (Completed)
- **2026-07-01** — Item drawer edit mode (branch `feature/item-drawer-edit`, spec in `context/features/item-drawer-edit-spec.md`). Added an **inline edit mode** to the item detail drawer: clicking Edit (pencil) swaps the action bar for **Save/Cancel** and turns the fields into editable inputs in the **same drawer** (no navigation). Cancel discards and returns to view mode; Save persists via a server action, exits edit mode, refreshes the drawer with the returned detail, and `router.refresh()`es the underlying card list. **Server action** `src/actions/items.ts` (new `src/actions/` file) → `updateItem(itemId, input)` following the `{ success, data?, error? }` shape: a Zod schema is the source of truth — `title` trimmed non-empty; `description`/`content`/`language` free text with `""`→`null` via a shared `emptyToNull` `z.preprocess`; `url` a valid-URL-or-null union (same preprocess); `tags` an array trimmed + blank-dropped, then **deduped** (`new Set`) before persisting so two identical tags don't collide on the `ItemTag` composite key. Returns the **first** Zod issue message in `{ success: false, error }` for client display; delegates persistence to the query layer (which enforces ownership), and maps a `null` result → "Item not found.". **Query** `src/lib/db/items.ts` → `updateItem(id, data)` + `UpdateItemData` interface: an **ownership check** (`findFirst({ where: { id, userId } })` via `requireUserId()`, `null` when not the user's) then `prisma.item.update` that replaces tags **wholesale** (`tags: { deleteMany: {}, create: … connectOrCreate by (name, userId) }`, so tags are reused across items), and returns the refreshed `ItemDetail` (via `getItemDetail`) so the drawer refreshes without a second fetch. **Edit form** `src/components/items/ItemEditForm.tsx` (client, presentational, controlled): renders the always-on fields (Title/Description/Tags) plus type-specific ones gated by `CONTENT_TYPES` (snippet/prompt/command/note → Content textarea), `LANGUAGE_TYPES` (snippet/command → Language input), and `link` (→ URL input); exposes `ItemEditState` (all raw strings, tags comma-separated) + an `initialEditState(detail)` seeder (seeds **every** field from detail regardless of type, so hidden fields round-trip their value rather than being nulled on save). Save/Cancel live in the drawer header, not the form. **Drawer** `src/components/items/ItemDrawer.tsx`: added `editing`/`form`/`saving` state; edit mode is **reset render-time** when the drawer switches items (via a `prevDetailId` compare, the React-recommended pattern — no effect, avoids the cascading-render lint rule) and on close; the header shows a live title preview (`form?.title || "Untitled"`); `handleSave` disables on empty title (client UX guard; Zod is the real gate), calls the action, `toast`s success/error, and threads the refreshed detail up via a new `onUpdated` prop. `item-drawer-context.tsx`'s `handleUpdated` sets the fresh `detail` **and** syncs the card `summary` (title/description/tags/favorite/pinned) so the header stays correct before re-render. **Toasts**: installed shadcn **sonner** (`src/components/ui/sonner.tsx`, dark-mode default since the app has no ThemeProvider) + shadcn **label**/**textarea**; mounted `<Toaster />` once in `AppShell`. **Unit tests** `src/actions/items.test.ts` (8 tests, the query layer mocked via `vi.hoisted`): empty/whitespace title rejected without touching the DB, invalid URL rejected, title-trim + empty→null collapse, valid URL passthrough + verbatim (untrimmed) content, tag trim/blank-drop/dedupe, success passthrough, null→not-found, thrown→generic error (20 total with the existing suites). **No DB schema/migration change** (uses existing `Item`/`Tag`/`ItemTag`). The pre-existing unrelated `.claude/skills/list-components/SKILL.md` working-tree edit was again left **out** of the commit. `npx tsc --noEmit`, `npm run lint`, `npm test` (20 pass), and `npm run build` all pass. (Completed)
- **2026-07-02** — Delete item (branch `feature/delete-item`). Made the item drawer's **Delete** action functional (it was display-only). **Server action** `src/actions/items.ts` → `deleteItem(itemId)` following the `{ success, error? }` shape: a small Zod `deleteItemSchema` (`{ itemId: z.string().trim().min(1) }`) is the source of truth, returning the first issue message on a bad payload; delegates to the query layer (which enforces ownership), maps a falsy result → "Item not found.", and wraps everything in try/catch → generic error. **Query** `src/lib/db/items.ts` → `deleteItem(id): Promise<boolean>`: `requireUserId()` + an **ownership `findFirst({ where: { id, userId } })` check** (returns `false` when the item isn't the user's, so another user's id can't delete it), then `prisma.item.delete`. `ItemCollection` / `ItemTag` join rows cascade on delete (`onDelete: Cascade`), so no manual cleanup. **Confirmation UI** `src/components/items/ItemDrawer.tsx`: the right-aligned Delete button now triggers a shadcn **`AlertDialog`** ("Delete this item?" naming the item title, "This action can't be undone") with `variant="destructive"` on the `AlertDialogAction` (forwarded to `Button`, per the profile-delete precedent — not a className override). `handleDelete` calls the action, `toast`s success ("Item deleted") / error, then `onDeleted()` closes the drawer and `router.refresh()` drops the item from the underlying card lists (dashboard + `/items/[type]`). The confirm button `preventDefault()`s so the dialog stays open while the delete is in flight (spinner + disabled Cancel/confirm via a `deleting` state); the drawer unmounts on success, and on error the dialog stays so the user can retry. **Wiring** `src/components/items/item-drawer-context.tsx`: added an `onDeleted`/`handleDeleted` callback (closes the drawer) threaded down to the drawer, mirroring the existing `onUpdated`/`handleUpdated` pattern. **Unit tests**: `src/actions/items.test.ts` — 4 `deleteItem` action cases (empty/whitespace id rejected without touching the DB, trimmed-id passthrough + success, false→not-found, thrown→generic error); `src/lib/db/items.test.ts` — 3 `deleteItem` **query** cases mirroring the `updateItem` query coverage (ownership-fail returns false without calling `prisma.item.delete`, `findFirst` scoped to `{ id, userId }`, and delete-by-id → true), with `item.delete` added to the hoisted prisma mock (27 total across the suites). **No DB schema/migration change** (uses existing `Item` + cascading joins). The pre-existing unrelated `.claude/skills/list-components/SKILL.md` working-tree edit (`argument-hint`) was again left **out** of the commit. `npx tsc --noEmit`, `npm run lint`, `npm test` (27 pass), and `npm run build` all pass. (Completed)
- **2026-07-02** — Item create (branch `feature/item-create`, spec in `context/features/item-create-spec.md`). Made the top bar's **New Item** button functional (it was disabled): it now opens a shadcn **Dialog** to create items of the **5 TEXT/URL types** (snippet, prompt, command, note, link — file/image excluded since they need R2 upload). **Query** `src/lib/db/items.ts` → `createItem(data: CreateItemData)` + `CreateItemData` interface: `requireUserId()`, resolves the system `ItemType` by raw name (`findFirst({ where: { name, isSystem: true } })`, **null** when not found → caller turns it into an error), derives `contentType` (`URL` for links, `TEXT` otherwise), and `prisma.item.create`s with tags via `connectOrCreate` on `(name, userId)` (so tags are reused across items, mirroring `updateItem`); returns the refreshed `ItemDetail` via `getItemDetail`. **Server action** `src/actions/items.ts` → `createItem(input)` following the `{ success, data?, error? }` shape: a Zod schema is the source of truth — `type` a `z.enum` of the 5 creatable types (the runtime gate; `CreateItemInput` widens `type` back to `string` so the client's type-state passes without pre-narrowing), `title` trimmed non-empty, `description`/`content`/`language` via the shared `nullableText` preprocess (`""`→`null`), `url` via `nullableUrl` (valid-URL-or-null), `tags` trimmed + blank-dropped, plus a `.refine` requiring a URL when `type === "link"`. The action then **nulls out fields that don't apply to the chosen type** (content only for the 4 text types, url only for link, language only for snippet/command) so a value typed before switching type isn't persisted, dedupes tags (`new Set`), and maps a `null` query result → "Could not create item.". **UI** `src/components/items/CreateItemDialog.tsx` (client): a `<Dialog>` whose `<DialogTrigger>` is the "New Item" button; a **type-selector** pill row (icon via `createElement(getTypeIcon(...))`, tinted by the type color when selected) sits above the **reused `ItemEditForm`** (so the conditional content/language/url fields already work per type). Opening resets to a blank form; submit calls the action, then `toast`s success/error, closes, and `router.refresh()`es so the new item appears in the dashboard/`/items/[type]` lists. `TopBar.tsx` now takes a `createTypes` prop (replacing the disabled New Item `<Button>`); `AppShell.tsx` derives it from the **already-fetched** `getSidebarItemTypes()` (filters out file/image, singular labels, DB-sourced icon/color) — **no new query**. **Unit tests**: `src/actions/items.test.ts` — 11 `createItem` **action** cases (empty title / non-creatable type / link-missing-url / invalid-url rejected without touching the DB; title-trim + empty→null + content-kept-for-text; link nulls content/language keeps url; note nulls language; tag trim/blank-drop/dedupe; success passthrough, null→"Could not create item.", thrown→generic); `src/lib/db/items.test.ts` — 3 `createItem` **query** cases (missing type → null without creating; TEXT contentType + scoped tag connect-or-create + refreshed detail; URL contentType for links), with `item.create` + `itemType.findFirst` added to the hoisted prisma mock (41 total across the suites). **No DB schema/migration change** (uses existing `Item`/`Tag`/`ItemTag`). The pre-existing unrelated `.claude/skills/list-components/SKILL.md` working-tree edit was again left **out** of the commit. `npx tsc --noEmit`, `npm run lint`, `npm test` (41 pass), and `npm run build` all pass. (Completed)
- **2026-07-02** — Code editor (Monaco) + type-specific create button (branch `feature/code-editor`, spec in `context/features/code-editor-spec.md`). Replaced the plain content `<pre>`/`<Textarea>` for **snippet & command** items with a Monaco-based code editor (display + inline edit); notes/prompts/links keep the plain `Textarea`. Installed `@monaco-editor/react` (v4.7; pulls `monaco-editor` transitively for types — loads editor assets from the jsDelivr CDN at runtime). **New component** `src/components/items/CodeEditor.tsx` (`"use client"`): a macOS-style **window header** (red/yellow/green traffic-light dots on the left; the **language label** + a functional **copy button** with a copied ✓ state on the right) over a Monaco `<Editor>`. Defines a custom `devstash-dark` theme in `beforeMount` (base `vs-dark`, surface `#171717` to match the app's dark card, muted gutter/line-numbers, subtle line highlight, and a translucent-white **themed scrollbar** via `scrollbarSlider.*`). **Fluid height**: floored at 48px, capped at **400px**, driven by `editor.getContentHeight()` on `onDidContentSizeChange` (scrolls internally past the cap); `scrollBeyondLastLine: false`, `automaticLayout: true`, minimap/folding/glyphs off, monospace font from `--font-geist-mono`. A `LANGUAGE_ALIASES` map + `toMonacoLanguage()` normalize the free-text `language` (e.g. `bash`→`shell`, `ts`→`typescript`) to a Monaco language id (falls back to `plaintext`); `languageLabel()` shows the raw language or "plain text". Both **read-only** (`readOnly`/`domReadOnly`, `renderLineHighlight: "none"`) and **editable** modes supported; options typed via `NonNullable<EditorProps["options"]>` to avoid importing the transitive `monaco-editor` package directly. **Wiring**: `ItemEditForm.tsx` — new `CODE_TYPES = {snippet, command}`; the Content field renders `CodeEditor` (passing `value.language` so highlighting updates live as the Language field changes) for code types, `Textarea` otherwise. `ItemDrawer.tsx` `ContentBlock` — snippet/command TEXT items render a read-only `CodeEditor`; other text keeps the `<pre>` block. **Type-specific create button** (added mid-feature at the user's request): each `/items/[type]` page now shows a **"New <Type>"** button in the header that opens the create modal with **that type preselected** (hidden on the non-creatable Pro-only file/image pages). `CreateItemDialog.tsx` gained `defaultType` (validated to be actually creatable, else falls back to the first type) + `triggerLabel` props; `initialType` seeds the type-state and the open-reset. Extracted the shared **`CreatableType`** interface + **`toCreatableTypes()`** pure helper (filters file/image, singular labels) into `src/lib/db/items.ts`; `AppShell.tsx` (derives from its already-fetched sidebar types — no extra query), `TopBar.tsx` (import moved to the data layer), and the type page (`Promise.all`s `getItemsByType` + `getSidebarItemTypes`) all use it. **Unit tests** `src/lib/db/items.test.ts` — 2 `toCreatableTypes` cases (excludes file/image; singular-label + icon/color passthrough) (43 total across the suites); the Monaco language helpers are component-internal so untested per the actions/`src/lib`-only testing rule. **No DB schema/migration change.** The pre-existing unrelated `.claude/skills/list-components/SKILL.md` working-tree edit (and a stray `delete-dialog.yml`) were again left **out** of the commit. Browser verification (Monaco loads from CDN at runtime) handed to the user. `npx tsc --noEmit`, `npm run lint`, `npm test` (43 pass), and `npm run build` all pass. (Completed)
- **2026-07-03** — Markdown editor (branch `feature/markdown-editor`, spec in `context/features/markdown-editor-spec.md`). Added a `MarkdownEditor` component with **Write/Preview** tabs for **note & prompt** content (snippet/command keep the Monaco `CodeEditor`; other types keep the plain `Textarea`). Installed `react-markdown` (v10) + `remark-gfm` (v4). **Component** `src/components/items/MarkdownEditor.tsx` (`"use client"`): a header (`bg-[#2d2d2d]`) over a body in a `bg-[#1e1e1e]` rounded/border container. In **edit** mode the header shows **Write**/**Preview** tab buttons and defaults to Write; **readonly** mode shows only a "Preview" label (Preview-only). A copy button on the right mirrors `CodeEditor`'s (Copy/✓ Copied, 1.5s reset, disabled when empty). **Write** is a `<textarea>` that auto-grows via a `useLayoutEffect` measuring `scrollHeight`, floored at 160px and capped at **400px** (then scrolls) — matching CodeEditor's fluid-height behavior. **Preview** renders GFM through `<ReactMarkdown remarkPlugins={[remarkGfm]}>` inside a `.markdown-preview` div (or a muted "Nothing to preview." when empty), in a `max-h-[400px]` scroll area. Props mirror `CodeEditor` (`value`/`onChange`/`readOnly`/`className`). **Styling** `src/app/globals.css`: a new **unlayered** `.markdown-preview` block (unlayered so it beats Tailwind's layered preflight) styling headings h1–h6 (distinct sizes/weights, underline on h1/h2), paragraphs, blue hover-underline links, disc/decimal lists (+ GFM task-list checkboxes via `:has()`), inline code (subtle `#ffffff14` bg) and `pre` code blocks (`#0d0d0d`), left-border blockquotes, bordered tables with a header bg, `hr`, images, and bold — all tuned for the fixed dark `#1e1e1e` surface. **Scrollbars**: a reusable `.themed-scrollbar` class (applied to the Write textarea + Preview scroll area, and scoped to `.markdown-preview pre`/`table`) reproduces Monaco's slider **exactly** — a **10px solid** thumb `#ffffff20` → hover `#ffffff33` → active `#ffffff40` on a transparent track, no arrow buttons. Key gotcha caught during review: modern Chrome **ignores `::-webkit-scrollbar-*` when the standard `scrollbar-width`/`scrollbar-color` are also set**, so those standard props were deliberately **omitted** (an earlier attempt that set both silently dropped the webkit styling — "nothing changed"). **Wiring**: `ItemEditForm.tsx` — new `MARKDOWN_TYPES = {note, prompt}`; the Content field now branches code → `CodeEditor`, markdown → `MarkdownEditor`, else `Textarea` (covers the create dialog **and** the drawer's inline edit, which both reuse this form). `ItemDrawer.tsx` `ContentBlock` — note/prompt TEXT items render a **readonly** `MarkdownEditor`; snippet/command keep the read-only `CodeEditor`; other text keeps the `<pre>` block. **No DB schema/migration change.** No unit tests — the feature is a React component + CSS + wiring (outside the server-actions/`src/lib` testing scope); the existing 43 tests still pass. Committed the deps + component + css + two wiring files + spec; the pre-existing unrelated `.claude/skills/list-components/SKILL.md` edit and stray `delete-dialog.yml` were again left **out**. Browser verification (Markdown render + scrollbar match) handed to the user. `npx tsc --noEmit`, `npm run lint`, `npm test` (43 pass), and `npm run build` all pass. (Completed)
- **2026-07-03** — File & image upload with Cloudflare R2 (branch `feature/file-image-upload`, spec in `context/features/file-image-spec.md`). Activated the two **Pro-only** system types (file, image) end-to-end, backed by R2 (S3-compatible) storage. Installed `@aws-sdk/client-s3`. **Storage core**: `src/lib/file-constraints.ts` — the single source of truth for upload rules (image **5 MB** / `.png,.jpg,.jpeg,.gif,.webp,.svg`; file **10 MB** / `.pdf,.txt,.md,.json,.yaml,.yml,.xml,.csv,.toml,.ini`), pure/tested (`validateFile` gates on **extension + size** as authoritative since browser MIME is unreliable for text formats; the spec's MIME list drives the client `accept` filter + the stored object Content-Type), plus `isFileCategory`/`fileExtension`/`acceptAttribute`/`formatMaxSize`. `src/lib/r2.ts` — lazily-built cached S3 client pointed at `https://<account>.r2.cloudflarestorage.com`; `uploadToR2`/`deleteFromR2ByUrl`/`getFromR2ByUrl`, `isR2Configured()`, and the pure key helpers `buildObjectKey` (`userId/uuid-slug`), `publicUrlForKey`, and `keyFromFileUrl` (the safety inverse — returns null for URLs outside our bucket so cleanup never deletes foreign objects). **API routes** (both `runtime = "nodejs"` — Buffer/SDK aren't edge-safe): `POST /api/upload` (auth + `validateFile` + push to R2, returns `{ fileUrl, fileName, fileSize }`; the item row is created separately by the action), and `GET /api/items/[id]/download` — an ownership-scoped (via `getItemDetail`) **stream proxy** (`Body.transformToWebStream()`) with `Content-Disposition: attachment` to force a download and sidestep CORS on the public bucket URL. **Data/action layer**: `src/lib/db/items.ts` — `ItemDetail` gains `fileUrl`; `createItem` derives `contentType: FILE` for file/image and persists the metadata (URL for links, TEXT otherwise unchanged); `deleteItem` best-effort removes the backing R2 object **after** the row delete (a storage hiccup won't fail the delete or leave a dangling row); `toCreatableTypes` now returns **all 7** system types (was 5), sorted by the canonical `TYPE_ORDER`. `src/actions/items.ts` — the `createItem` action adds `file`/`image` to the Zod creatable enum + `fileUrl`/`fileName`/`fileSize` fields, a `.refine` requiring an uploaded file for file/image, and nulls out fields that don't apply to the chosen type. **UI**: new `src/components/items/FileUpload.tsx` (`"use client"`) — drag-and-drop zone that uploads on selection via **XMLHttpRequest** (not `fetch`, so it can render an `upload.onprogress` progress bar), then shows an image preview or file-info card with remove-to-replace; `CreateItemDialog.tsx` renders it for file/image, auto-fills the title from the filename, and gates submit on a completed upload; `ItemDrawer.tsx`'s `ContentBlock` for FILE items shows an image preview (images) + a file-info card and a **Download** button (→ the proxy route) for all files. `/items/files` & `/items/images` pages now get a working "New File/Image" button since those types became creatable. **Editing** a file item (drawer) only touches title/description/tags — file fields are untouched, so the object is preserved. **Unit tests**: `src/lib/file-constraints.test.ts` (validation/extension/size), `src/lib/r2.test.ts` (key helpers, notably the `keyFromFileUrl` bucket-boundary guard), and extended `db/items.test.ts` + `actions/items.test.ts` (FILE create + contentType, R2 cleanup called only when `fileUrl` present, file-required refine, field-nulling); suite **43 → 64**. Decisions/limitations: **no Pro gating** on upload (consistent with the whole app — all dev users are Pro; gating deferred), MIME **not** strictly enforced server-side (extension is), and an **orphaned R2 object** is possible if a user uploads then cancels (out of scope). R2 env vars (`R2_ACCOUNT_ID`/`R2_ACCESS_KEY_ID`/`R2_SECRET_ACCESS_KEY`/`R2_BUCKET_NAME`/`R2_PUBLIC_URL`) were already set in `.env`; image preview requires the bucket to be public at `R2_PUBLIC_URL`. **No DB schema/migration change** (the `fileUrl`/`fileName`/`fileSize` columns already existed on `Item`). Fixed stale "5 creatable / file-image excluded" comments in `items.ts`, `AppShell.tsx`, and `items/[type]/page.tsx`. The pre-existing unrelated `.claude/skills/list-components/SKILL.md` edit and stray `delete-dialog.yml` were again left **out** of the commit. Live R2 round-trip verification (upload → preview → download → delete-cleans-R2) handed to the user. `npx tsc --noEmit`, `npm run lint`, `npm test` (64 pass), and `npm run build` all pass (both new routes register as `ƒ Dynamic`). (Completed)
- **2026-07-04** — Image gallery view (branch `feature/image-gallery-view`, spec in `context/features/image-display-spec.md`). Render **image** items as a thumbnail gallery on `/items/images` instead of the standard list card. **New component** `src/components/items/ImageCard.tsx` (`"use client"`): a card with a 16:9 (`aspect-video`) thumbnail using `object-cover` (edges crop to fill) and a subtle **hover zoom** (`group-hover:scale-105`, `duration-300`), a caption strip below (title + pin/star), and `openItem(item)` on click to open the existing item drawer. **Wiring** `src/app/items/[type]/page.tsx`: branch on `itemType.name === "image"` — the same responsive `grid gap-3 md:grid-cols-2 lg:grid-cols-3` (3 cols at `lg`, per spec) now renders `ImageCard` for images and the unchanged `ItemCard` for every other type. **Data**: surfaced `fileUrl` on the `DashboardItem` view model (`items.ts` interface + `ItemRow` type + `toDashboardItem` mapper) so the thumbnail has a source; the column was already fetched by the shared `itemInclude`, so no query change. **R2 flakiness fix** (raised during browser verification — thumbnails intermittently `ERR_CONNECTION_RESET` from Cloudflare's throttled public `r2.dev` URL): stopped displaying images from that URL. **New route** `GET /api/items/[id]/image` (`runtime = "nodejs"`) streams the object **inline** from R2's **S3 API endpoint** (via the existing `getFromR2ByUrl`), behind the same session-scoped ownership check as the download route, with `Cache-Control: private, max-age=86400, immutable` (an item's file never changes). `ImageCard` and the `ItemDrawer` FILE image preview now use `/api/items/${id}/image` (ImageCard adds an `onError` fallback to the type icon); `FileUpload`'s create-dialog preview switched to a **local `URL.createObjectURL`** blob (zero-network, revoked on change/unmount) since there's no item id pre-create. Net effect: images now serve reliably (verified in Playwright — all thumbnails 200 across repeated reloads, zero console errors) **and** stay auth-gated (not publicly reachable via a bucket URL). Prod: works as-is provided the 5 `R2_*` vars are set (they are in `.env.production`) and `R2_PUBLIC_URL` matches the prefix stored on items' `fileUrl` (used by `keyFromFileUrl` to derive the object key); an optional future upgrade is a Cloudflare **custom domain** on the bucket for edge-CDN public images (tradeoff: loses the per-user privacy the proxy gives). **No DB schema/migration change** (`fileUrl` column already existed). No unit tests — the feature is a React component + route wiring (outside the server-actions/`src/lib` testing scope); the existing 64 tests still pass. Verified end-to-end via Playwright (signed in as the demo user, uploaded test images of varied aspect ratios to exercise the crop, confirmed the drawer opens from the new card). The pre-existing unrelated `.claude/skills/list-components/SKILL.md` edit and stray `delete-dialog.yml` were again left **out** of the commit. `npx tsc --noEmit`, `npm run lint`, `npm test` (64 pass), and `npm run build` all pass (`/api/items/[id]/image` registers as `ƒ Dynamic`). (Completed)
- **2026-07-04** — File list view (branch `feature/file-list-view`, spec in `context/features/file-display-spec.md`). Render **file** items on `/items/files` as a single-column list (Google Drive / Dropbox style) instead of grid cards; image items keep the gallery grid and every other type keeps the standard `ItemCard` grid. **New component** `src/components/items/FileRow.tsx` (`"use client"`): a full-width row with a left border tinted by the type color and a hover highlight (`hover:bg-accent/40`), laid out as an extension-based icon chip + file name + size + upload date, with a **download** button on the right. The icon maps the file extension to a Lucide glyph (`.pdf/.txt/.md` → `FileText`, `.json` → `FileJson`, `.yaml/.yml/.xml/.toml/.ini` → `FileCode`, `.csv` → `FileSpreadsheet`, fallback `File`) via the existing `fileExtension` helper. The row's icon+info area is a `<button>` calling `openItem(item)` (opens the existing item drawer); the download is a **sibling** `<a href="/api/items/[id]/download" download>` (valid HTML — no interactive nesting) with `onClick` `stopPropagation` so downloading doesn't also open the drawer. It streams via the existing session-scoped download proxy route (`Content-Disposition: attachment`). **Responsive**: the info block is `flex-col` on mobile (name/size/date stacked) and `sm:flex-row` columns on larger screens; the download button is always visible on mobile but hover-revealed (`sm:opacity-0 sm:group-hover:opacity-100`) on desktop. **Data**: surfaced `fileName` + `fileSize` on the `DashboardItem` view model (`items.ts` interface + `ItemRow` type + `toDashboardItem` mapper) so a row can show the real filename and size — both columns were already fetched by the shared `itemInclude` (which uses `include`, not a scalar `select`), so **no query change** (mirrors how the image-gallery feature added `fileUrl`). **Utility** `src/lib/file-constraints.ts` → new `formatFileSize(bytes)` that scales the unit (`B`/`KB`/`MB`/`GB`, one decimal dropped when whole, `—` for null/negative) — the existing `formatMaxSize` rounds to whole MB, too coarse for real file sizes. **Wiring** `src/app/items/[type]/page.tsx`: added an `isFileList` branch (`itemType.name === "file"`) that renders a `flex flex-col gap-2` list of `FileRow`; the image-gallery and standard-grid branches are unchanged. **Unit tests** `src/lib/file-constraints.test.ts` — 4 new `formatFileSize` cases (unit scaling, whole-value decimal drop, one-decimal rounding, null/undefined/negative → `—`); suite **64 → 68**. **No DB schema/migration change** (`fileName`/`fileSize` columns already existed on `Item`). The commit message was first mangled by a PowerShell here-string pasted into the Bash tool (subject prefixed with a stray `@`) and immediately fixed via `git commit --amend`. The pre-existing unrelated `.claude/skills/list-components/SKILL.md` edit and stray `delete-dialog.yml` were again left **out** of the commit. Live browser verification (a signed-in user with real uploaded file items → row renders, drawer opens, download fires) offered but not run in-session. `npx tsc --noEmit`, `npm run lint`, `npm test` (68 pass), and `npm run build` all pass (`/items/[type]` stays `ƒ Dynamic`). (Completed)
- **2026-07-04** — Item title above filename in file list rows (commit `d938236`, `feat:`). Small presentational tweak to `src/components/items/FileRow.tsx`: the file row's name column now shows the item's **`title`** as the primary label with the original **`fileName`** as a muted secondary line beneath it (only when `fileName` is set), instead of showing just the filename. Wrapped the single name `<span>` in a `flex flex-col` so both lines truncate independently and the size/date columns still spread across on `sm+`. One-file change — no data-layer, schema, or query change (`title`/`fileName` already on the `DashboardItem` view model). No unit tests (CSS/markup-only presentational change). (Completed)
- **2026-07-04** — Hover copy button on dashboard item cards (commit `b9511f3`, `feat:`). Added a hover-revealed **Copy** button to the dashboard `ItemCard` (`src/components/dashboard/ItemCard.tsx`). **New component** `src/components/items/CopyItemButton.tsx` (`"use client"`): shown only for the 5 copyable types (`snippet`/`prompt`/`command`/`note`/`link`; hidden for file/image which have no copyable text via a `COPYABLE_TYPES` set); since the card view-model doesn't carry item content, it **fetches full detail on click** from the existing on-demand `/api/items/[id]` route and copies `content ?? url` to the clipboard, with a `Check`-icon "copied" state (1.5s reset) and `sonner` toasts on empty/error. Always visible on mobile, `sm:opacity-0 sm:group-hover:opacity-100` on desktop. **Card restructure**: `ItemCard`'s root changed from a `<button>` to a `<div>` with an **absolutely-positioned overlay `<button>`** as the full-card open-drawer target, so the copy button can sit as a **sibling on top** rather than an (invalid) nested button; the icon/content/time got `pointer-events-none relative` so clicks fall through to the overlay while the copy button (`relative`, above the overlay) stays clickable, and `handleCopy` `stopPropagation`s so copying doesn't also open the drawer. No data-layer/schema change. No unit tests (client component; outside the server-actions/`src/lib` testing scope). (Completed)
- **2026-07-04** — Dedupe `formatFileSize` into shared util (branch `refactor/dedupe-format-file-size`, commit `2d2c140`, `refactor:`). Removed the two near-duplicate local `formatFileSize` implementations in `src/components/items/FileUpload.tsx` and `src/components/items/ItemDrawer.tsx` in favor of the canonical `formatFileSize` from `@/lib/file-constraints` (added in the file-list-view feature), which additionally handles `null`/`undefined`/negative (→ `—`) and drops the trailing `.0` on whole values. Net −24 lines; both files now import the shared helper. No behavior change beyond the util's null/whole-value handling; no data-layer, schema, or query change. Covered by the existing `formatFileSize` unit tests in `src/lib/file-constraints.test.ts` (68 pass). (Completed)
- **2026-07-12** — Audit quick-win fixes (branch `feature/audit-quick-wins`). Actioned the four low-risk items from the 2026-07-11 code-scanner audit (report: `.claude/agent-memory/nextjs-codebase-auditor/audit-findings-2026-07-11.md`). **1. SVG hardening** — added `X-Content-Type-Options: nosniff` to the item **image** route (`src/app/api/items/[id]/image/route.ts`) so an uploaded `.svg` served `Content-Disposition: inline` can't execute embedded `<script>` on direct navigation (self-XSS today since routes are ownership-scoped with no sharing, but becomes real if item-sharing ships). **2. Leaner file-streaming fetcher** — added `getItemFileRef(id)` + `ItemFileRef` interface to `src/lib/db/items.ts` (user-scoped `findFirst` selecting only `{ contentType, fileUrl, fileName }`) and swapped both the **download** and **image** routes off the heavier `getItemDetail()` (which joins itemType + tags + collections) — drops those joins on every download/thumbnail request while keeping the identical `{ id, userId }` ownership scoping. **3. `deleteAccount` R2 cleanup** (`src/actions/profile.ts`) — the account-delete cascade removes `Item` rows but not their backing R2 objects, orphaning every file/image a deleted user uploaded (unbounded storage leak). Now collects the user's `fileUrl`s (`where: { userId, fileUrl: { not: null } }`) **before** the delete `$transaction`, then best-effort `deleteFromR2ByUrl` each via `Promise.allSettled` **after** (a storage hiccup can't fail the account deletion) — mirroring the existing single-item `deleteItem` precedent. **4. Redundant `token.id as string` cast in `src/auth.ts`** — the audit flagged it as noise, but it turned out to be a **false positive**: removing the cast fails `tsc` (`TS2322: Type '{}' is not assignable to type 'string'`) because the `JWT` module augmentation in `next-auth.d.ts` doesn't flow into NextAuth v5's `session` callback, where `token.id` resolves to `{}`. The cast is **load-bearing**; reverted the change and saved a memory so it isn't re-flagged. **Unit tests**: 3 new `getItemFileRef` cases (null / user+field scoping / passthrough) in `src/lib/db/items.test.ts`, and a new `src/actions/profile.test.ts` with 6 `deleteAccount` cases (not-found guard, file lookup scoping, R2 cleanup per file after delete, no-files → no R2 calls, best-effort — an R2 rejection still succeeds, transaction-throw → generic error); suite **68 → 77**. **No DB schema/migration change.** The unrelated pre-existing `.claude/**` working-tree edits (agent-memory + `list-components/SKILL.md`) were again left **out** of the commit. `npx tsc --noEmit`, `npm run lint`, `npm test` (77 pass), and `npm run build` all pass. (Completed)
- **2026-07-13** — Refactor: split `ItemDrawer` + dedup `items.ts` mappers (branch `refactor/split-item-drawer`, commit `47ece2b`, `refactor:`). Structural cleanup of the two largest hand-written files surfaced by a file-size review, no behavior change. **Split `ItemDrawer.tsx` (600 → 316 lines)** into orchestration plus focused pieces: `src/components/items/ItemContentBlock.tsx` (the URL/FILE/code/markdown/plain content switch — renamed from the internal `ContentBlock` to exported `ItemContentBlock`), `src/components/items/ItemActionBar.tsx` (`"use client"` — the action bar + its `ActionButton` helper + the delete `AlertDialog`, renamed `ActionBar` → `ItemActionBar`), `src/components/items/drawer-primitives.tsx` (the `Section` + `DetailRow` layout primitives), `src/components/items/item-detail-response.ts` (the `ItemDetailResponse` wire type — `ItemDetail` with ISO-string dates — moved to its own module so the extracted components can import it without a cycle back through `ItemDrawer`), and `src/lib/format.ts` (`formatFullDate`, the "January 15, 2024" formatter). `ItemDrawer.tsx` keeps only container state/orchestration (open/edit/save/delete handlers) and **re-exports** `ItemDetailResponse`, so `item-drawer-context.tsx` (its only importer) needed no change. **Dedup `src/lib/db/items.ts` (488 → 483 lines, kept as one file)**: added a single `toItemType(row)` mapper that builds the `DashboardItemType` view model (adds the capitalized `label`), replacing **three** copy-pasted inline projections (`toDashboardItem`, `getItemsByType`, `getItemDetail`); and `getItemDetail` now spreads the shared `itemInclude` (+ its `collections` select) instead of re-inlining the `itemType`/`tags` select shape. `toLabel` stays imported (still used by `getSidebarItemTypes` + `toCreatableTypes`, which have different label shapes). **No new files split out of `items.ts`** (it's a cohesive data-access layer). Considered but intentionally left as-is: `SidebarContent.tsx` (293) and `actions/items.ts` (230) — reasonable size, modest payoff. **Pure refactor: no behavior, schema/migration, or dependency changes**; the existing `src/lib/db/items.test.ts` mapper coverage still passes unchanged (77 tests). The commit subject was first mangled by a PowerShell here-string (`@'…'@`) pasted into the Bash tool (Git Bash), prefixing the subject with a stray `@` — fixed immediately via `git commit --amend -F -` with a bash heredoc. The unrelated pre-existing `.claude/**` working-tree edits (agent-memory + `list-components/SKILL.md`) were again left **out** of the commit. `npx tsc --noEmit`, `npm run lint`, `npm test` (77 pass), and `npm run build` all pass. (Completed)
- **2026-07-14** — Collection create (branch `feature/collection-create`, commit `8fbdc80`, `feat:`). Made the top bar's **New Collection** button functional (it was a disabled placeholder): it now opens a shadcn **Dialog** to create a user-scoped collection with a **name** (required) + optional **description**, mirroring the item-create stack across all three layers. **Query** `src/lib/db/collections.ts` → `createCollection(data: CreateCollectionData)` + `CreateCollectionData` interface: `requireUserId()` + `prisma.collection.create` scoped to that `userId`, returning a `DashboardCollection` view model with `itemCount: 0`, empty `types`, and the default `primaryColor: "var(--border)"` (a brand-new collection has no items to derive a type color from — matching the existing `getDashboardCollections` fallback). **Server action** `src/actions/collections.ts` (new file, mirrors `src/actions/items.ts`) → `createCollection(input)` following the `{ success, data?, error? }` shape: a Zod schema is the source of truth (`name` trimmed non-empty; `description` `""`→`null` via the shared `emptyToNull`/`nullableText` preprocess), returning the first Zod issue message on invalid input and a generic message on a thrown error; delegates persistence to the query layer. **Client dialog** `src/components/items/CreateCollectionDialog.tsx` (`"use client"`): a `<Dialog>` whose trigger is the outline **New Collection** button, with a Name `Input` + Description `Textarea`; opening resets to a blank form, submit calls the action then `toast`s success/error, closes, and `router.refresh()`es so the new collection appears in the dashboard grid + sidebar (both `createdAt desc`, so it sorts to the top). Submit is guarded on a non-empty name (client UX; Zod is the real gate). **Wiring** `TopBar.tsx`: replaced the disabled placeholder `<Button>` (+ dropped the now-unused `FolderPlus` import) with `<CreateCollectionDialog />`; no new query needed (creation needs no preloaded data). Per the established items pattern the **create mutation goes through the server action**, not a REST API route (reads stay in server components / `lib/db`) — flagged this against the spec's "api routes for any client-side calls" wording and confirmed the server-action approach for consistency. **Unit tests**: `src/actions/collections.test.ts` — 6 action cases (empty/whitespace name rejected without touching the DB, name trim, empty description→null, description passthrough, success result, thrown→generic error); `src/lib/db/collections.test.ts` (new) — 3 query cases (user-scoped create, `DashboardCollection` view-model shape, null description), prisma/auth mocked via `vi.hoisted`; suite **77 → 86**. **No DB schema/migration change** — the `Collection` model already exists. The unrelated pre-existing `.claude/**` working-tree edits (agent-memory + `list-components/SKILL.md`) were again left **out** of the commit. `npx tsc --noEmit`, `npm run lint`, `npm test` (86 pass), and `npm run build` all pass. (Completed)
- **2026-07-14** — Add item to collections (branch `feature/add-item-to-collections`, commit `935b662`, `feat:`). Let a user assign an item to **one or many collections** from the item forms — a collection selector now appears in both the **new-item dialog** and the **drawer edit** mode (they share `ItemEditForm`). Memberships persist via the existing **`ItemCollection`** join table, so **no DB schema/migration change**; `getItemDetail` already returned `collections: { id, name }[]` (the drawer displayed them read-only) — this makes them settable/editable. **Data — options** `src/lib/db/collections.ts` → new lightweight `getCollectionOptions()` + `CollectionOption` (`{ id, name }[]`, user-scoped via `requireUserId()`, alphabetical), kept separate from the heavier sidebar/dashboard fetchers. **Data — persistence** `src/lib/db/items.ts`: `collectionIds: string[]` added to `CreateItemData` + `UpdateItemData`; a module-private **`ownedCollectionIds(ids, userId)`** filters the requested ids to the ones the user actually owns (`prisma.collection.findMany({ where: { id: { in: ids }, userId } })`) — a **security guard**, since the `ItemCollection` FK alone would accept another user's collection id. `createItem` connects the owned ids (`collections: { create: [{ collection: { connect: { id } } }] }`); `updateItem` replaces memberships **wholesale** (`collections: { deleteMany: {}, create: … }`), mirroring the tag pattern. **Actions** `src/actions/items.ts`: `collectionIds` (array, `.default([])`, trim + drop-blank; **deduped** via `new Set` in the action so identical ids don't collide on the `ItemCollection` composite key) added to both `createItemSchema` + `updateItemSchema` and threaded into the query data. **Forms** `src/components/items/ItemEditForm.tsx`: `collectionIds: string[]` added to `ItemEditState` (seeded in `initialEditState` from `detail.collections`), plus a `collections: CollectionOption[]` prop; the selector renders for **all** item types. **UI evolved during the feature** at the user's request: started as toggle-chips, then switched to a **dropdown multi-select** (shadcn `DropdownMenu` + `DropdownMenuCheckboxItem`, trigger shows the comma-joined selected names / muted placeholder, `onSelect` preventDefault so the menu stays open across multiple toggles); as part of the same pass the create dialog's **Type** selector was also converted from a pill row to a **dropdown menu** (`DropdownMenuItem` per type, colored icon + label). Empty state: "No collections yet — create one from the top bar." **Wiring** (options threaded server → client as props, matching the `createTypes` precedent — **no new API route**): `AppShell.tsx` adds `getCollectionOptions()` to its `Promise.all` and passes the result to both `TopBar` (→ `CreateItemDialog`) and `ItemDrawerProvider` (→ `ItemDrawer` → `ItemEditForm`); `TopBar.tsx` forwards a `collections` prop; `CreateItemDialog.tsx` seeds `collectionIds: []` in `BLANK_FORM` and includes it in the `createItem` call; `item-drawer-context.tsx` accepts + forwards `collections`, and `ItemDrawer`'s `handleSave` includes `form.collectionIds` in the `updateItem` payload; the `/items/[type]` page (which also renders `CreateItemDialog`) fetches options too. As with collection-create, the mutation goes through the **server action**, not a REST route (flagged against the spec's "api routes for any client-side calls" wording, confirmed with the user). Out of scope (per spec): collection detail pages / listing items inside a collection. **Unit tests**: `src/lib/db/collections.test.ts` — `getCollectionOptions` (user-scoped, alphabetical `{ id, name }[]`); `src/lib/db/items.test.ts` — `createItem` / `updateItem` connect **only owned** collection ids (foreign id filtered out; `collection.findMany` added to the prisma mock) + updated exact-match assertions for the `collections` write; `src/actions/items.test.ts` — `collectionIds` trim/blank-drop/dedupe pass-through for create + update; suite **86 → 91**. The unrelated pre-existing `.claude/**` working-tree edits (agent-memory + `list-components/SKILL.md`) were again left **out** of the commit. `npx tsc --noEmit`, `npm run lint`, `npm test` (91 pass), and `npm run build` all pass. Live browser verification (select collections on create/edit, confirm membership persists) offered but not run in-session. (Completed)
- **2026-07-14** — Collections pages (branch `feature/collections-pages`, commit `f1ea474`, `feat:`). Built the `/collections` list page and the `/collections/[id]` detail page (the missing targets for the sidebar's "View all collections" link and the collection cards — both of which **already** linked to these routes from prior work, so no link wiring was needed). **Query** `src/lib/db/collections.ts` → `getCollectionDetail(id)` + `CollectionDetail` / `CollectionDetailType` interfaces: a user-scoped `prisma.collection.findFirst({ where: { id, userId } })` (unknown/foreign id → `null` → page `notFound()`) that includes its items via the `ItemCollection` join (`orderBy: { addedAt: "desc" }`), hydrated through the **shared item include/mapper** — to enable that, `itemInclude` + `toDashboardItem` (and the `DashboardItemType` type) are now **exported** from `src/lib/db/items.ts` and imported into `collections.ts` (no N+1). The detail also computes the **distinct item types present, each with its item count** (`CollectionDetailType extends DashboardItemType { count }`), ordered by frequency (most-common first), derived from the already-fetched items (no extra query). **List page** `src/app/collections/page.tsx` (server, `force-dynamic`): reuses `getDashboardCollections(100)` + the existing `CollectionCard` in the same `grid sm:grid-cols-2 lg:grid-cols-3`, with a dashed empty state. **Detail page** `src/app/collections/[id]/page.tsx` (server, `force-dynamic`, `params` is a `Promise` per Next 16): a header (name + favorite `Star`, item count, description, and a row of **type-count chips** — icon + label + muted count via `createElement(getTypeIcon(...))`), then items **grouped into three sections** so files/images render like their `/items/[type]` pages: standard types in the `ItemCard` grid (no heading), an **Images** section (`ImageCard` gallery grid), and a **Files** section (`FileRow` single-column list), each shown only when non-empty; a dashed empty state when the collection has no items. **Layout** `src/app/collections/layout.tsx` reuses the shared `AppShell` (sidebar + top bar + the mounted `ItemDrawerProvider`, so clicking a card opens the existing item drawer). **Auth**: `proxy.ts` adds `/collections` to both `PROTECTED_PREFIXES` and the `matcher`. The feature evolved over the session per user requests: started with `ItemCard` for all items → then image/file specialized cards → then images **and** files each split into their own labeled section → then per-type **counts** added to the header chips. **Unit tests** `src/lib/db/collections.test.ts` — 4 `getCollectionDetail` cases (null/`{ id, userId }` scoping, full view-model mapping incl. type-count, distinct-types frequency ordering with per-type counts, empty collection → `itemCount 0`/empty arrays); suite **91 → 95**. **No DB schema/migration change** (uses existing `Collection`/`ItemCollection`). The unrelated pre-existing `.claude/**` working-tree edits (agent-memory + `list-components/SKILL.md`) were again left **out** of the commit. Live browser verification offered but not run in-session. `npx tsc --noEmit`, `npm run lint`, `npm test` (95 pass), and `npm run build` all pass (`/collections` + `/collections/[id]` register as `ƒ Dynamic`). (Completed)
- **2026-07-14** — Collection edit / delete / favorite actions (branch `feature/collection-actions`, commit `252c970`, `feat:`). Added Edit / Delete / Favorite controls to collections — in the `/collections/[id]` detail-page header **and** on the collection cards (dashboard + `/collections`) — mirroring the item CRUD stack across all three layers. **Query** `src/lib/db/collections.ts`: refactored the distinct-type tally out of `getDashboardCollections` into a shared **`buildDashboardCollection(row)`** helper + a `dashboardCollectionInclude` constant (so every fetcher/mutator returning a `DashboardCollection` stays consistent); added `updateCollection(id, data)` + `UpdateCollectionData` (ownership check via `findFirst({ where: { id, userId } })` → `null` when not the user's, else `prisma.collection.update` name/description and return the **refreshed** view model via the shared include/helper) and `deleteCollection(id): Promise<boolean>` (same ownership check, then `prisma.collection.delete` — the collection's **items are preserved**, only the `ItemCollection` join rows cascade away via the join table's `onDelete: Cascade`; **no manual item cleanup**). **Server actions** `src/actions/collections.ts`: `updateCollection(collectionId, input)` + `deleteCollection(collectionId)` following the `{ success, data?, error? }` shape — Zod is the source of truth (`updateCollectionSchema` = name trimmed non-empty + description `""`→null via the existing `nullableText` preprocess; `deleteCollectionSchema` = trimmed non-empty id), returning the first issue message on bad input, `"Collection not found."` on a null/false query result, and a generic message on a thrown error. **Components** (all new): `EditCollectionDialog.tsx` — a **controlled** shadcn `Dialog` (open state owned by the parent so the trigger can live in a card menu or a header) editing name/description, seeded from the collection on open via the **render-time state-reset pattern** (`prevOpen` compare, not a `useEffect` — avoids the `react-hooks/set-state-in-effect` lint rule), calls the `updateCollection` action then toasts + closes + `router.refresh()`; `DeleteCollectionDialog.tsx` — a **controlled** shadcn `AlertDialog` confirming deletion (copy explicitly notes items are **not** deleted), `variant="destructive"` action that `preventDefault`s to stay open while the delete is in flight, with an `onDeleted` override (default `router.refresh()`); `CollectionCardMenu.tsx` (`"use client"`) — the **⋯ dropdown** (`DropdownMenu`) with Edit / Favorite / Delete, owning the two dialogs' open state, sitting as a `relative z-10` sibling with `stopPropagation` on the trigger so it never triggers card navigation; `CollectionDetailActions.tsx` (`"use client"`) — the detail-header Edit / Favorite / Delete icon buttons, where **delete navigates back to `/collections`** (the collection no longer exists) via the `onDeleted` override. **`CollectionCard.tsx` restructured** (stayed a server component): root changed from a `<Link>` wrapper to a `<div>` with an **absolute overlay `<Link>`** as the full-card nav target + the `CollectionCardMenu` as a `pointer-events-auto` sibling on top, and the static content marked `pointer-events-none` — same overlay pattern as `ItemCard`'s hover copy button, so clicking anywhere but the ⋯ menu still navigates. Detail page (`src/app/collections/[id]/page.tsx`) header wrapped in a `flex justify-between` to seat `CollectionDetailActions` top-right. **Favorite is display-only this pass** on both surfaces (the icon reflects `isFavorite` but has no mutation — a placeholder for a later feature, per spec). **Unit tests**: `src/actions/collections.test.ts` — 10 new (updateCollection: empty-name reject / trim + empty-desc→null / desc passthrough / success / null→not-found / throw→generic; deleteCollection: empty-id reject / trim + success / false→not-found / throw→generic); `src/lib/db/collections.test.ts` — 4 new (updateCollection query: not-owner → null without updating / updates + returns frequency-ordered view model with `primaryColor`; deleteCollection query: not-owner → false without deleting / owned delete-by-id → true), with `collection.update`/`collection.delete` added to the hoisted prisma mock; suite **95 → 109**. **No DB schema/migration change** (uses existing `Collection` + `ItemCollection` cascade). The unrelated pre-existing `.claude/**` working-tree edits (agent-memory + `list-components/SKILL.md`) were again left **out** of the commit. Live browser verification offered but not run in-session. `npx tsc --noEmit`, `npm run lint`, `npm test` (109 pass), and `npm run build` all pass (`/collections` + `/collections/[id]` stay `ƒ Dynamic`). (Completed)
- **2026-07-14** — Global search / command palette (branch `feature/global-search`, spec in `context/features/global-search-spec.md`, commit `dd31861`). Built a **Cmd/Ctrl+K command palette** with client-side fuzzy search across the current user's items and collections, grouped into **Items** and **Collections** sections. **Data**: new `src/lib/db/search.ts` → `getSearchData()` (scoped via `requireUserId()`) pre-fetches a **bounded** dataset once on app load — items via the shared `itemInclude`/`toDashboardItem` (so results are full `DashboardItem`s and can open the existing drawer directly) plus a truncated 200-char `contentPreview`, and collections as `{ id, name, itemCount }` (item count from `_count`). Caps: `take: 500` items / `200` collections; view-model types `SearchItem` (extends `DashboardItem`) / `SearchCollection` / `SearchData`. Search runs **entirely in the browser** — no per-keystroke round-trips (spec: no server round-trips). **State/wiring**: new `src/components/search/search-context.tsx` (`SearchProvider` owns the palette `open` state + the global Cmd/Ctrl+K keydown listener + the pre-fetched data; `useSearch()` hook) wraps the whole shell; `AppShell.tsx` adds `getSearchData()` to its `Promise.all`, wraps the shell in `SearchProvider`, and mounts `<CommandPalette />` **inside** the `ItemDrawerProvider` (so selecting an item reuses `openItem`) — the palette consumes both `useSearch` and `useItemDrawer`. **Palette** `src/components/search/CommandPalette.tsx` (shadcn **cmdk** `Command`): grouped Items (type-tinted icon via `createElement(getTypeIcon(...))`, title, type label in a `CommandShortcut`) + Collections (Folder icon, name, `N items`); selecting an item opens the drawer, a collection `router.push`es to `/collections/[id]`; cmdk provides fuzzy matching + arrow/Enter keyboard nav. **TopBar**: replaced the disabled search `<Input>` with a button that opens the palette and shows a **platform-aware shortcut hint** (`Ctrl K` / `⌘K`, resolved via `useSyncExternalStore` with a server snapshot of `false` to avoid a hydration mismatch **and** the `react-hooks/set-state-in-effect` lint rule that a `useEffect`+`setState` approach tripped). **Two fixes along the way**: (1) the shadcn-generated `ui/command.tsx` `CommandDialog` was **missing the `<Command>` root wrapper** around `{children}`, so every cmdk primitive (`CommandInput`/`List`/`Item`/…) threw on open ("lots of errors" on click) — added the wrapper and also let `CommandDialog` **forward a `filter` prop** to `<Command>`; (2) cmdk's default filter fuzzy-matches any **subsequence**, so with the content preview as a keyword, typing "test" matched almost every item (t‑e‑s‑t scattered through the body). Added a **custom filter** `src/lib/search-filter.ts` (`commandFilter` + `isSubsequence`) that restricts fuzzy (subsequence) matching to the **short label** (`keywords[0]` = title/name) and matches the longer tags+content preview by **literal substring only** — tiered scores (label-prefix 1 / label-contains 0.9 / label-subsequence 0.7 / extra-substring 0.5 / else 0-hidden); `CommandItem` `value` is now just the id (identity) with `keywords=[title, ...tags, contentPreview]`. **Unit tests**: `src/lib/db/search.test.ts` (4 — user scoping of both queries, item→`SearchItem` mapping, preview truncation + null-content tolerance, collection `_count` mapping) and `src/lib/search-filter.test.ts` (10 — subsequence helper, the score tiers, ranking order, and a direct **"test matches everything" regression** proving a scattered-subsequence content match now scores 0); suite **109 → 123**. **Verified end-to-end via Playwright** (signed in as the demo user): palette opens on click **and** Ctrl+K, grouped results, fuzzy filter ("debounce" → useDebounce hook), Enter opens the item drawer, selecting a collection navigates, "test" now returns 2 tight results (was ~all 25), Ctrl K hint renders on Windows, console 0 errors. Installed `cmdk` (via `npx shadcn add command`, which also generated `ui/input-group.tsx`). **No DB schema/migration change.** The unrelated pre-existing `.claude/**` working-tree edits (agent-memory + `list-components/SKILL.md`) were again left **out** of the commit. `npx tsc --noEmit`, `npm run lint`, `npm test` (123 pass), and `npm run build` all pass (`/dashboard` stays `ƒ Dynamic`). (Completed)
- **2026-07-14** — Pagination (branch `feature/pagination`, spec in `context/features/pagination-spec.md`, commit `d28fa22`). Added **server-side pagination** to the `/items/[type]`, `/collections/[id]`, and `/collections` list pages — each request fetches only one page of rows (`skip`/`take` + a `count`) instead of loading everything, and the page number comes from a `?page=` URL search param so the pages stay SSR/shareable. **Shared module** `src/lib/pagination.ts`: the four spec constants (`ITEMS_PER_PAGE = 21`, `COLLECTIONS_PER_PAGE = 21`, `DASHBOARD_COLLECTIONS_LIMIT = 6`, `DASHBOARD_RECENT_ITEMS_LIMIT = 10`) plus three pure helpers — `parsePageParam` (invalid/missing/`<1` → 1; takes the first entry of a repeated param), `getPagination(requestedPage, totalCount, perPage)` → `{ page, perPage, totalCount, totalPages, hasPrev, hasNext, skip, take }` (clamps `page` into `[1, totalPages]` so an out-of-range `?page=` never yields a negative skip or an empty past-the-end page; empty set → `totalPages 1`), and `pageSequence(current, totalPages, siblings=1)` (first/last + a window, `"ellipsis"` markers for gaps). **Component** `src/components/pagination/Pagination.tsx` — a **server component** (just `<Link>`s, no `'use client'`): Prev / numbered pages / Next, where Prev/Next at the ends render as greyed-out non-interactive `<span>`s, the current page is an `aria-current` highlighted span, ellipses are inert; renders **nothing** when `totalPages <= 1`; page-1 links use the clean base path (no `?page=1`). Takes `page` / `totalPages` / `basePath`. **Fetchers** (`src/lib/db`): `getItemsByType(slug, page)` now does `item.count` + `skip`/`take` (was `take: 100`) and returns `totalCount` / `page` / `totalPages` on the `ItemsByType` view model; `getCollectionDetail(id, page)` paginates the collection's items via `itemCollection.count` + a `skip`/`take` `itemCollection.findMany`, but keeps the **header type chips reflecting the whole collection** (not just the current page) via a separate `item.groupBy({ by: ["itemTypeId"], where: { collections: { some: { collectionId } } } })` aggregate + an `itemType.findMany` for the present types — added `page`/`totalPages` and switched `itemCount` to the true total; new `getCollectionsPage(page)` + `CollectionsPage` interface for the `/collections` list (`collection.count` + paginated `findMany` through the shared `dashboardCollectionInclude`/`buildDashboardCollection`, replacing the old `getDashboardCollections(100)` that loaded everything). **Constant mapping** (a judgment call beyond the spec's literal pairing, confirmed by adding `/collections`): `ITEMS_PER_PAGE` governs every *item* list — `/items/[type]` **and** the items inside `/collections/[id]` — while `COLLECTIONS_PER_PAGE` governs the *collections* list (`/collections`); so the collection-detail item list was moved from `COLLECTIONS_PER_PAGE` → `ITEMS_PER_PAGE` (identical value, names that read correctly). **Pages**: `/items/[type]`, `/collections/[id]`, and `/collections` each `await searchParams`, pass `parsePageParam(page)` into their fetcher, show the true total in the header, and render `<Pagination>` at the bottom (`basePath` = the list's path). The dashboard's `getDashboardCollections(6)` / `getRecentItems(10)` were rewired to `DASHBOARD_COLLECTIONS_LIMIT` / `DASHBOARD_RECENT_ITEMS_LIMIT` (fetcher default params too). The `/collections` addition came from mid-review user feedback ("pagination was not implemented on … list all collections"). **Unit tests**: new `src/lib/pagination.test.ts` (15 — `parsePageParam` clamping/array, `getPagination` middle/clamp-high/clamp-low/empty/round-up, `pageSequence` no-gap/windowed/edge-trim/single); extended `items.test.ts` (getItemsByType: no count/fetch on bad slug, first-page metadata + scoped count/`skip`/`take`, page-2 slice, out-of-range clamp) and `collections.test.ts` (getCollectionDetail rewritten for the count+groupBy+findMany surface incl. whole-collection chip counts + page slice; new `getCollectionsPage` cases) — the fetcher slice-assertions derive expected `skip`/`take` from `getPagination`/the constants rather than hardcoding `21`, so they stay correct at any page size (they passed while the constants were temporarily set to `2` for manual browser testing, then restored to spec values). Suite **123 → 143**. **Browser-verified** via Playwright (signed in as the demo user; temporarily seeded 25 extra snippets on the Neon **development** branch — endpoint identity confirmed first — via a throwaway `scripts/pgtest.ts`, deleted after): `/items/snippets` page 1 shows 21 with Prev greyed + Next active, page 2 shows the remaining 7 with Prev active + Next greyed, `?page=99` clamps to the last page, collection detail keeps its whole-collection type chip; **zero console errors**. **No DB schema/migration change.** The unrelated pre-existing `.claude/**` working-tree edits (agent-memory + `list-components/SKILL.md`) were again left **out** of the commit. `npx tsc --noEmit`, `npm run lint`, `npm test` (143 pass), and `npm run build` all pass (`/items/[type]`, `/collections`, `/collections/[id]` stay `ƒ Dynamic`). (Completed)
- **2026-07-15** — Settings page (branch `feature/settings-page`, commit `5e92da0`). Added a protected `/settings` page and **moved the account actions** (Change password + Delete account) there from the profile page. **Page** `src/app/settings/page.tsx` (server, `dynamic = "force-dynamic"`, defense-in-depth `auth()` redirect guard like the profile page) renders the **Account Settings** card — `ChangePasswordDialog` (gated on `user.hasPassword` via `getProfileUser()`, so it's hidden for GitHub OAuth-only users) + `DeleteAccountDialog` — with a Settings header. **Layout** `src/app/settings/layout.tsx` reuses the shared `AppShell` (sidebar + top bar), mirroring `profile/layout.tsx`, so `/settings` renders inside the dashboard chrome. **Profile page** `src/app/profile/page.tsx`: removed the entire Account Settings `<section>` (+ the now-unused `ChangePasswordDialog`/`DeleteAccountDialog` imports); it now shows only Account Information + Usage Statistics. **Sidebar** `src/components/dashboard/SidebarContent.tsx`: added a **Settings** `DropdownMenuItem` (→ `/settings`, `Settings` lucide icon, `onClick={closeMobile}`) to the user-avatar dropdown, between Profile and Sign out. **Protection** `src/proxy.ts`: added `/settings` to both `PROTECTED_PREFIXES` and the `matcher`. **Component move**: `git mv`'d `ChangePasswordDialog.tsx`, `ChangePasswordForm.tsx`, `DeleteAccountDialog.tsx` from `src/components/profile/` → `src/components/settings/` (folder now empty/removed) and updated the three import paths (the settings page's two imports + `ChangePasswordDialog`'s import of `ChangePasswordForm`) — done at the user's request after the initial implementation left them in `components/profile/`. Interpreted the request's "forgot password" as the profile's **Change password** action (there is no forgot-password control on the authenticated profile — Forgot Password is the unauthenticated `/forgot-password` flow). **No new tests** — pure UI move + wiring; the underlying `changePassword`/`deleteAccount` server actions are unchanged (existing suite of 143 still passes). **No DB schema/migration change.** The unrelated pre-existing `.claude/**` working-tree edits (agent-memory + `list-components/SKILL.md`) were again left **out** of the commit. `npx tsc --noEmit`, `npm run lint`, `npm test` (143 pass), and `npm run build` all pass (`/settings` registers as `ƒ Dynamic`). (Completed)
- **2026-07-15** — Editor preferences settings (branch `feature/editor-preferences`, spec in `context/features/editor-settings-spec.md`, commit `c89cd09`). Added an **Editor Preferences** section to `/settings` that persists per-user Monaco editor settings and **auto-saves on change** (no save button). **Schema/migration**: new `editorPreferences Json?` column on `User` (null = app defaults), via `prisma migrate dev` (migration `20260715085406_add_editor_preferences`, Neon **development** branch only). **Single source of truth** `src/lib/editor-preferences.ts`: the curated dropdown options (`FONT_SIZE_OPTIONS`, `TAB_SIZE_OPTIONS`, `EDITOR_THEMES`), the `EditorPreferences` interface, `DEFAULT_EDITOR_PREFERENCES` (word-wrap **on**, minimap **off**, theme `vs-dark`, 13px/2-space to match CodeEditor's prior hard-coded values), a strict Zod `editorPreferencesSchema` (numeric fields accept a **wider range** than the dropdowns so a stored value just outside the list still round-trips), and a tolerant `normalizeEditorPreferences(raw)` that coerces the untrusted JSON column (null/partial/legacy) into a complete object, filling missing/invalid fields from the defaults (never throws). **Server action** `src/actions/editor-preferences.ts` → `updateEditorPreferences(input)` following the `{ success, data?, error? }` shape: Zod-validated, `requireUserId()`-scoped, replaces the stored value wholesale (the client sends the full object). **Fetcher** `src/lib/db/user.ts` → `getEditorPreferences()` reads + normalizes on app load. **Context/provider** `src/components/editor/editor-preferences-context.tsx` → `EditorPreferencesProvider` (mounted in `AppShell`, seeded from `getEditorPreferences()` via a new entry in its `Promise.all`) holds the current preferences and a `update(partial)` that optimistically merges + **debounces** the persist (600ms, coalescing a burst of toggles into one write) then toasts success/error via `sonner`; a default context value means a stray consumer outside the provider still reads sane defaults and never crashes. `useEditorPreferences()` hook exposes `{ preferences, update }`. **Settings UI** `src/components/settings/EditorPreferencesSection.tsx` (rendered on the settings page below Account Settings): font-size + tab-size + theme **Select** dropdowns and word-wrap + minimap **Switch** toggles, each wired to `update({ … })` — no save button. **CodeEditor** `src/components/items/CodeEditor.tsx` now reads prefs from the context and applies `fontSize`/`lineHeight`/`tabSize`/`wordWrap`/`minimap`; the previously hard-coded single `devstash-dark` theme was replaced by **three registered Monaco themes** (`devstash-vs-dark` = the app's neutral-900 surface, `devstash-monokai` = `#272822`, `devstash-github-dark` = `#0d1117`), each defined in `beforeMount` (base `vs-dark` + surface/token/scrollbar overrides) and selected by `preferences.theme`; the container background is matched to the active theme's surface so there's no flash while Monaco loads. Installed shadcn **select** + **switch** (`src/components/ui/select.tsx`, `switch.tsx`). **Unit tests**: `src/lib/editor-preferences.test.ts` (7 — `normalizeEditorPreferences`: null/non-object → defaults, valid passthrough, partial fill, invalid-field fallback, valid partial kept, unknown keys ignored) and `src/actions/editor-preferences.test.ts` (6 — valid persist scoped to the user + echoed back, defaults accepted, unknown theme / out-of-range font / non-integer tab rejected without a DB write, DB-throw → generic error), prisma/`requireUserId` mocked via `vi.hoisted`; suite **143 → 156**. The unrelated pre-existing `.claude/**` working-tree edits (agent-memory + `list-components/SKILL.md`) and stray browser-test artifacts (`*.yml`, `*.png`) were again left **out** of the commit. Live browser verification (change each control → toast + persist across reload; open a snippet → editor reflects theme/font/minimap) offered but not run in-session. `npx tsc --noEmit`, `npm run lint`, `npm test` (156 pass), and `npm run build` all pass (`/settings` stays `ƒ Dynamic`). (Completed)
- **2026-07-15** — Favorites page (branch `feature/favorites-page`, spec in `context/features/favorites-spec.md`, commit `cbc0212`). Added a protected `/favorites` page listing the current user's favorited **items** and **collections** as a compact, dense list (VS Code / terminal style — `font-mono` rows, thin dividers, subtle `hover:bg-accent/40`, no cards). **Data** (`src/lib/db`): `getFavoriteItems()` (`items.ts`) and `getFavoriteCollections()` (`collections.ts`), both scoped via `requireUserId()`, filtering `isFavorite: true`, ordered by `updatedAt desc` (`take: 100`), reusing the shared `itemInclude` / `toDashboardItem` and `dashboardCollectionInclude` / `buildDashboardCollection` (no N+1). They return new `FavoriteItem` / `FavoriteCollection` view models that **extend** `DashboardItem` / `DashboardCollection` with `updatedAt` — which stands in for "favorited at" (the schema has **no per-favorite timestamp**), used as both the sort key and the row's displayed date (decision carried from the spec's open question). Because `FavoriteItem` is a structural superset of `DashboardItem`, a row passes it straight to the item drawer. **Page** `src/app/favorites/page.tsx` (server, `dynamic = "force-dynamic"`, `Promise.all`s both fetchers): a star-chip header, then **two sections** (Items · N, Collections · N) each a divided bordered list, and a single dashed **empty state** when the user has neither. **Rows** (`src/components/favorites/`): `FavoriteItemRow.tsx` (`"use client"`) — type-tinted icon (`createElement(getTypeIcon(...))`), title, type badge, short date; `onClick` → `useItemDrawer().openItem(item)` (full detail fetched on demand in the drawer). `FavoriteCollectionRow.tsx` (server) — a type-tinted `Folder`, name, item-count badge, short date, wrapped in a `<Link href="/collections/[id]">`. **TopBar** `src/components/dashboard/TopBar.tsx`: a ghost **star icon button** (`<Link href="/favorites">`) added left of the New Collection / New Item controls. **Layout** `src/app/favorites/layout.tsx` reuses the shared `AppShell` (sidebar + top bar + `ItemDrawerProvider`, so item rows open the drawer). **Utility** `src/lib/format.ts` → new `formatShortDate` ("Jan 15, 2024") for the dense rows. **Protection** `src/proxy.ts`: `/favorites` added to both `PROTECTED_PREFIXES` and the `matcher`. **Unit tests**: `getFavoriteItems` / `getFavoriteCollections` coverage added to `src/lib/db/items.test.ts` + `collections.test.ts` (user + `isFavorite` scoping, `updatedAt desc` order, view-model mapping incl. `updatedAt`); suite **156 → 160**. **No DB schema/migration change** (uses the existing `Item.isFavorite` / `Collection.isFavorite` columns). The unrelated pre-existing `.claude/**` working-tree edits (agent-memory + `list-components/SKILL.md`) were again left **out** of the commit; stray browser-test artifacts from the prior session were deleted first. `npx tsc --noEmit`, `npm run lint`, `npm test` (160 pass), and `npm run build` all pass (`/favorites` registers as `ƒ Dynamic`). (Completed)
- **2026-07-16** — Favorite toggle across surfaces (branch `feature/favorite-toggle`, inline spec, commit `f6478fb`). Made the previously **display-only** Favorite star actually toggle and persist, on the three surfaces from the request: the **item drawer**, the **collection detail page**, and the **collection cards**. Two open questions were confirmed with the user at `start`: item **cards get no toggle button** (toggling stays drawer-only, but a toggled item's favorite state must *display* on the dashboard `ItemCard` + list views), and **favorite only** — Pin stays display-only. **Query layer** (both ownership-scoped via `requireUserId()` + a `findFirst({ where: { id, userId } })` check, then `update` setting `isFavorite`, returning `boolean`): `setItemFavorite(id, isFavorite)` in `src/lib/db/items.ts` and `setCollectionFavorite(id, isFavorite)` in `src/lib/db/collections.ts` — another user's id (or unknown id) returns `false`. **Server actions** (`{ success, error? }` shape, Zod-validated `{ id (trimmed, non-empty), isFavorite (boolean) }`, delegate to the query layer, map `false` → "Item/Collection not found."): `setItemFavorite` in `src/actions/items.ts`, `setCollectionFavorite` in `src/actions/collections.ts`. **Client — item drawer**: the toggle logic lives in `item-drawer-context.tsx` (the container that owns `detail`/`summary`), added a `handleToggleFavorite` that computes `next`, optimistically flips `isFavorite` on **both** `detail` and `summary` (id-guarded functional `setState`), calls the action, **reverts + `toast.error`s on failure**, else `router.refresh()`; a `favoritePending` flag guards against overlapping requests. Threaded `onToggleFavorite` + `favoritePending` down `ItemDrawer` → `ItemActionBar`, where the Favorite `ActionButton` gained `onClick`/`disabled` and a label that flips **Favorite ↔ Unfavorite**. **Client — collections**: `CollectionDetailActions.tsx` (`/collections/[id]` header) and `CollectionCardMenu.tsx` (dashboard + `/collections` card ⋯ menu) each got local optimistic `isFavorite`/`favoritePending` state, an async `toggleFavorite` (optimistic → action → revert-on-failure + toast → `router.refresh()`); the card menu's `DropdownMenuItem` keeps the menu open across the toggle (`e.preventDefault()`), and `CollectionDetailActions`'s local `ActionButton` gained `disabled` support. **Display**: dashboard `ItemCard` and `ImageCard` already rendered a favorite star, so a drawer toggle + `router.refresh()` reflects there automatically; added the missing star indicator to `FileRow.tsx` so the file list view shows it too (satisfying the "display on list views" requirement). **Unit tests** (+17, suite **160 → 177**): `setItemFavorite`/`setCollectionFavorite` **actions** (empty/whitespace id rejected without a DB call, trimmed-id success, `false`-flag pass-through, `false`→not-found, throw→generic — 5 each) and **queries** (ownership-fail → `false` without `update`, `findFirst` scoped to `{ id, userId }`, update-by-id sets the flag → `true`, false-flag pass-through — 4 item / 3 collection). The client components (drawer context, `ItemActionBar`, `CollectionCardMenu`, `CollectionDetailActions`, `FileRow`) are untested per the actions/`src/lib`-only testing rule. **No DB schema/migration change** (`Item.isFavorite` / `Collection.isFavorite` already exist). The unrelated pre-existing `.claude/**` working-tree edits (agent-memory + `list-components/SKILL.md`) were again left **out** of the commit. `npx tsc --noEmit`, `npm run lint`, `npm test` (177 pass), and `npm run build` all pass. Live browser verification (toggle in drawer + collection card → persists, shows on `/favorites` + sidebar) offered but not run in-session. (Completed)
- **2026-07-16** — Favorites page sorting (branch `feature/favorites-sorting`, commit `5ac1fa2`). Added **client-side sorting** to the `/favorites` page. Per decisions confirmed at start: **per-section controls** (the Items and Collections sections each get their own sort dropdown + direction toggle), **type sort is items-only** (Items: name/date/type; Collections: name/date — a collection has no single type), **default date-desc with an asc/desc toggle**, and the choice **persists to localStorage**. **Pure utility** `src/lib/favorites-sort.ts` (only type-only imports of the view models, so no server code leaks into the client bundle and it's unit-testable): `sortFavoriteItems(items, field, direction)` + `sortFavoriteCollections(collections, field, direction)` — both non-mutating (`[...arr].sort(...)`), name compared case-insensitively via `localeCompare({ sensitivity: "base" })`, date by `updatedAt.getTime()`, and **type by a local canonical `TYPE_ORDER`** (snippet→prompt→command→note→file→image→link, mirroring the sidebar order; unknown types sort last, ties broken by name); `direction === "desc"` reverses the ascending result. Also exports the `SortDirection`/`ItemSortField`/`CollectionSortField` types, a `SortPreference<F>` interface, and the `ITEM_SORT_FIELDS`/`COLLECTION_SORT_FIELDS` dropdown option lists. **localStorage hook** `src/components/favorites/use-persistent-sort.ts` (`usePersistentSort(key, allowedFields, fallback)`): backs the preference with **`useSyncExternalStore`** (matching the `TopBar` isMac precedent) so the server + hydrating client render the fallback and correct to the stored value **after** hydration — no hydration mismatch and **no `react-hooks/set-state-in-effect`** trip. The snapshot is the raw `"field:direction"` string (stable by primitive identity, so no snapshot-cache loop); it's parsed via `useMemo` and validated against `allowedFields`, falling back on anything malformed. Writes go through `localStorage.setItem` + a manual module-level listener `notify` (same-tab), and the `storage` event covers cross-tab; the server snapshot is `null` and reads/writes are `try/catch`-guarded (private mode/quota). **UI**: `FavoritesSortControl.tsx` — a presentational, reusable control (shadcn `Select size="sm"` for the field + an outline `size-8` `Button` toggling `ArrowUp`/`ArrowDown` with an `aria-label`); `FavoriteItemsSection.tsx` + `FavoriteCollectionsSection.tsx` — `'use client'` sections that own their preference (localStorage keys `favorites:items:sort` / `favorites:collections:sort`), `useMemo` the sorted list, and render the **existing** `FavoriteItemRow` / `FavoriteCollectionRow` unchanged. **Page** `src/app/favorites/page.tsx` stays a server component — still `Promise.all`s `getFavoriteItems()` + `getFavoriteCollections()` and just passes the rows down to the two client sections (the old inline `<section>` markup was replaced), keeping data-fetching server-side per the SSR preference (`Date` fields cross the RSC boundary fine, as the existing rows already relied on). **Unit tests** `src/lib/favorites-sort.test.ts` — 13 cases (items: no-mutation, name asc/desc case-insensitive, date asc/desc, type by `TYPE_ORDER`, type ties→name, unknown-type-last; collections: no-mutation, name asc/desc, date asc/desc); suite **177 → 190**. **No DB schema/migration change** (reuses the existing `getFavoriteItems`/`getFavoriteCollections` fetchers and their `updatedAt` field). The unrelated pre-existing `.claude/**` working-tree edits (agent-memory + `list-components/SKILL.md`) were again left **out** of the commit. `npx tsc --noEmit`, `npm run lint`, `npm test` (190 pass), and `npm run build` all pass (`/favorites` stays `ƒ Dynamic`). Live browser verification (exercise both controls, confirm persistence across reload) offered but not run in-session. (Completed)
- **2026-07-16** — Pinned items (branch `feature/pinned-items`, spec in `context/features/pinned-spec.md`, commit `97c90e4`). Made the previously **display-only** Pin button in the item drawer actually toggle and persist `Item.isPinned`, and sorted pinned items to the top of listings — a near-clone of the 2026-07-16 favorite-toggle stack across all three layers. **Query** `src/lib/db/items.ts` → `setItemPinned(id, isPinned): Promise<boolean>` (ownership-scoped via `requireUserId()` + a `findFirst({ where: { id, userId } })` check, then `prisma.item.update` setting `isPinned` — another user's/unknown id returns `false`), mirroring `setItemFavorite`. **Server action** `src/actions/items.ts` → `setItemPin(itemId, isPinned)` following the `{ success, error? }` shape (Zod-validated `{ itemId (trimmed, non-empty), isPinned (boolean) }`, delegates to the query layer, maps `false` → "Item not found.", generic message on throw). **Pinned-first sort**: `getItemsByType` (the `/items/[type]` list) changed from `orderBy: { createdAt: "desc" }` to `orderBy: [{ isPinned: "desc" }, { createdAt: "desc" }]`; the **collection-detail** item list (`src/lib/db/collections.ts`) changed from `orderBy: { addedAt: "desc" }` to `orderBy: [{ item: { isPinned: "desc" } }, { addedAt: "desc" }]` (ordering the `ItemCollection` rows by the joined item's flag). The dashboard **Recent** list was left as pure `createdAt desc` recency (the dashboard already has a dedicated Pinned section fed by the existing `getPinnedItems`). **Client — drawer**: `item-drawer-context.tsx` gained `handleTogglePin` + a `pinPending` guard — optimistically flips `isPinned` on **both** `detail` and `summary` (id-guarded functional `setState`), calls the action, **reverts + `toast.error`s on failure**, else `router.refresh()` (so listings re-sort); threaded `onTogglePin` + `pinPending` down `ItemDrawer` → `ItemActionBar`, where the Pin `ActionButton` gained `onClick`/`disabled`, a label that flips **Pin ↔ Unpin**, and an `activeClassName="fill-foreground text-foreground"` so the icon fills when pinned. **Display-only, unchanged** (per spec): the Pin icon on the dashboard `ItemCard` stays a **static indicator** (no toggle button on cards — it already rendered `item.isPinned` via a `<Pin>`); Pin is items-only (no collection pinning). **Unit tests** (+9, suite **190 → 199**): `setItemPinned` **query** (ownership-fail → `false` without `update`, `findFirst` scoped to `{ id, userId }`, update-by-id sets the flag → `true`, false-flag pass-through — 4) and `setItemPin` **action** (empty/whitespace id rejected without a DB call, trimmed-id success, `false`-flag pass-through, `false`→not-found, throw→generic — 5); also updated the collection-detail test's exact `orderBy` assertion to the new pinned-first array. The client components (drawer context, `ItemActionBar`) are untested per the actions/`src/lib`-only testing rule. **No DB schema/migration change** (`Item.isPinned` already exists on the schema + view models). The unrelated pre-existing `.claude/**` working-tree edits (agent-memory + `list-components/SKILL.md`) were again left **out** of the commit. `npx tsc --noEmit`, `npm run lint`, `npm test` (199 pass), and `npm run build` all pass. Live browser verification (pin in drawer → button flips to Unpin + fills, item jumps to top of its `/items/[type]` list, persists on reload) offered but not run in-session. (Completed)
- **2026-07-16** — Homepage mockup (branch `feature/homepage-mockup`, spec in `context/features/homepage-mockup-spec.md`, commit `787f2e3`). Built a standalone **marketing homepage prototype** under `prototypes/homepage/` — vanilla `index.html` / `styles.css` / `script.js`, **separate from the Next.js `src/` app** (no framework, no build step, no DB; not part of `npm run build`, verified in a browser via a throwaway Python static server on :8791). **Hero (main focus)** is a "chaos → order" visual in three parts: a **chaos field** of 8 labeled source icons (Notion, GitHub, Slack, VS Code, browser tabs, terminal, text file, bookmark) that drift, bounce off the walls, pulse/rotate, and **repel from the cursor** via `requestAnimationFrame` (each icon is a wrapper that *translates* containing a *tile* that *rotates+scales* and an *upright label* — so labels never spin; the animation only runs while the hero is on-screen via an IntersectionObserver, honors `prefers-reduced-motion`, and caps velocity/repel force); a **CSS-pulsing transform arrow**; and a **dashboard preview** that reads like the real app — branded + labeled sidebar rail (Snippets/Prompts/Commands/Notes/Links with type-colored icons), a search top bar with avatar, a **"Recent · 24 items"** section heading, and real **item cards** (type icon chip + title + accent-colored tag pills + colored top border). **Other sections**: fixed nav (grows opaque on scroll), gradient hero headline + CTAs, **6 type-tinted feature cards** on a lighter **full-bleed band** (via a `100vw` `::before`) for visual separation, an **AI section** (Pro badge + capability checklist + a code-editor mockup with staggered "AI Generated Tags"), **Free/Pro pricing** with a **monthly/yearly toggle** (JS swaps `data-monthly`/`data-yearly` text → $8/mo vs $6/mo "$72 billed once a year") and a "Most Popular" badge, a CTA band, and a footer with the **current year** injected in JS. **JS also** handles navbar scroll opacity and scroll-reveal fade-ins (IntersectionObserver, `.reveal` → `.in-view`). Dark theme on the item-type accent palette; **text and primary buttons use the snippet blue** (`#3b82f6`) rather than purple/indigo (brand graphics — logo, hero arrow, Pro-card identity — kept their existing gradient). **Responsive**: mobile stacks the hero vertically with the **arrow rotated 90°** (points down) and collapses all grids to single column; nav links + ghost button hide on small screens. Iterated live with the user across several passes (enriched the sparse dashboard mockup with real cards + top bar + labeled rail + section heading, added chaos-icon labels + "8 apps. Zero organization." / "7 types. One fast search." captions, enlarged the chaos icons and softened the cursor repel, added the Features separation band, and switched text/buttons from purple to blue). No unit tests — static HTML/CSS/JS with no server-actions/`src/lib` surface (the only tested scope); the existing suite (199) is unaffected. **No DB schema/migration change.** The unrelated pre-existing `.claude/**` working-tree edits (agent-memory + `list-components/SKILL.md`) were again left **out** of the commit. Browser-verified end-to-end (desktop 1280 + mobile 390: hero animation, chaos labels upright, dashboard mockup, pricing toggle $6/$72, arrow 90° rotation, all sections; console clean apart from a harmless favicon 404). (Completed)
- **2026-07-16** — Homepage (branch `feature/homepage`, spec in `context/features/homepage-spec.md`, commit `098b5b8`). Ported the standalone `prototypes/homepage/` prototype into the real Next.js app as the **public landing page at `/`**, replacing the placeholder `page.tsx` (`<h1>Devstash</h1>`). Rebuilt as React/Tailwind v4 + shadcn, split into server + client components under `src/components/marketing/`, with all repeated UI driven by co-located `const` arrays in **`data.ts`** (nav links, chaos icons, 6 feature cards, 2 pricing tiers + their feature lists, 3 footer columns, AI capabilities/tags). **Public route** — `/` is **not** in `proxy.ts`'s `PROTECTED_PREFIXES`, so no auth-gate change was needed; reuses the root `layout.tsx` chrome (`<html class="dark">` + Geist fonts). **Server components** (static markup): `Hero` (composes the showcase), `DashboardPreview` (the "…with DevStash" mini-dashboard mockup — labeled sidebar rail + search top bar + Recent heading + 4 accent-bordered item cards, all inline arrays), `Features` (6 accent-tinted cards on a lighter **full-bleed band** via the `.mk-band` `100vw ::before`), `AiSection` (Pro badge + capability checklist + a syntax-highlighted `useDebounce.ts` editor mockup whose "AI Generated Tags" stagger in), `Pricing` (shell + `SectionHead`), `Cta`, `SiteFooter` (footer year via `new Date().getFullYear()` on the server), plus shared `Brand` (DS logo/wordmark), `MarketingButton` (shadcn `Button asChild` restyled to the prototype's gradient-primary / bordered-ghost look — uses the `ghost` base variant so the gradient background-image sits above and the `dark:hover:` overrides are neutralized), and `SectionHead`. **Client components** (`'use client'`, interactivity only): `SiteNav` (fixed nav; scroll listener toggles opacity/blur past 24px; nav links hidden `md:` down, ghost "Sign In" hidden `max-[560px]`), `ChaosField` (the hero **chaos field** — 8 labeled source icons that drift, bounce off the walls, gently pulse/rotate, and **repel from the cursor** via `requestAnimationFrame`; each icon = a wrapper that *translates* + inner `[data-tile]` that *rotates/scales* + an *upright label*; a single `useEffect` ports the prototype's physics, gates the RAF behind an IntersectionObserver so it only runs on-screen, honors `prefers-reduced-motion` by bailing entirely, and cleans up all listeners/RAF/observer on unmount), `BillingToggle` (controlled monthly/yearly segmented control), `PricingPlans` (owns the billing `period` state — added beyond the spec's component list because the toggle and the reactive Pro price can't share client state across the server/client boundary; renders the toggle + both tier cards, Pro price swaps $8/"/mo"/"Billed monthly." ↔ $6/"/mo billed yearly"/"$72 billed once a year."), and `Reveal` (IntersectionObserver scroll-in fade wrapper that adds `.mk-in`; the AI tag stagger keys off the same class; the rare no-IO fallback reveals via `requestAnimationFrame` to avoid the `react-hooks/set-state-in-effect` lint rule). **`globals.css`**: added the **item-type accent palette** (`--color-snippet` `#3b82f6`, prompt `#f59e0b`, command `#06b6d4`, note `#22c55e`, file `#64748b`, image `#ec4899`, url `#6366f1`) **+ the dark marketing surface scale** (`--color-mk-bg`/`elevated`/`surface`/`surface-2`/`border`/`border-strong`/`text`/`muted`/`dim`) to a new `@theme` block, exposing utilities like `bg-snippet`, `text-mk-muted`, `border-mk-border`; and a marketing CSS block (keyframes `mk-arrow-pulse`/`mk-tag-in` + hard-to-express selectors — the ambient hero glow `.mk-page::before`, the chaos grid overlay `.mk-chaos-grid::after`, the full-bleed `.mk-band::before`, gradient text, the reveal/tag-stagger states, the mobile arrow-rotate keyframe, and reduced-motion overrides) all scoped under `.mk-page` so none of it leaks into the app shell. Data-driven per-card accents use `style={{ "--c": color } as React.CSSProperties}` + `color-mix(in_srgb,var(--c) …)` arbitrary values. Brand marks lucide lacks (Notion, Slack) added as tiny local SVGs in `brand-icons.tsx` (GitHub reuses the auth `GitHubIcon`; VS Code + generics use lucide, matching the prototype). **Fixed** an 8px horizontal overflow (the `100vw` band including the scrollbar gutter) with `overflow-x-hidden` on the page wrapper. **CTA routing** (per user request mid-feature): the initial build had an optional signed-in variant (`auth()` → "Go to Dashboard"); the user asked to drop it, so the nav always shows **Sign In → `/sign-in`** + **Get Started → `/register`** and the hero shows **Get Started Free → `/register`** — the `auth()` call was removed, making `/` a **static** route (`○`) again. Pricing/CTA buttons → `/register`; in-page anchors (Features/Pricing/See Features) use plain `<a href="#…">`, route links use `next/link` (via `MarketingButton`/`Brand`). **No unit tests** — presentational page with no server-actions/`src/lib` surface (the only tested scope); the existing suite (199) is unaffected. **No DB schema/migration change.** Kept `prototypes/homepage/` as reference (not deleted). The unrelated pre-existing `.claude/**` working-tree edits (agent-memory + `list-components/SKILL.md`) were again left **out** of the commit. **Browser-verified** end-to-end via Playwright (desktop 1280 + mobile 390): hero chaos/arrow/preview, Features cards, AI section with animated tags, Pricing, CTA, current-year footer; billing toggle → $6/"/mo billed yearly"/"$72 billed once a year"; nav "Sign In"→`/sign-in` + "Get Started"→`/register`, hero "Get Started Free"→`/register`; mobile nav links hidden + arrow computed `rotate(90°)` (points down); zero horizontal overflow; **zero console errors/warnings**. `npx tsc --noEmit`, `npm run lint`, and `npm run build` all pass (`/` registers as `○ Static`). (Completed)
- **2026-07-17** — Mobile layout fixes + marketing top bar on the auth pages (branch `fix/mobile-layout-and-auth-nav`, merged to `main` as `7b5b44d`). Four user-reported issues, committed separately. **1. AI/Pro section snippet clipped on mobile** (`fix:` `d6cb2ae`, `src/components/marketing/AiSection.tsx`) — at 390px the code card rendered **507px wide inside a 327px grid column**, spilling to x=531 and getting clipped by the homepage wrapper's `overflow-x-hidden`, so most of the snippet was off-screen. The `<pre>`'s existing `overflow-x-auto` never engaged because a grid item defaults to **`min-width: auto`**, letting the column widen to fit the longest code line instead of forcing the `pre` to scroll. Fixed with **`min-w-0`** on the `Reveal` grid item; the card now fits its column and the `pre` scrolls (`scrollWidth` 505 vs `clientWidth` 325). Desktop unchanged (564px, no scroll). **2. Dashboard top bar unreachable on mobile** (`fix:` `b89a218`, `TopBar.tsx` + both create dialogs) — the header is a single non-wrapping flex row whose content measured **709px at a 390px viewport**, pushing the New Collection / New Item group to x=433, entirely off-screen. Added an **opt-in `compact` prop** to `CreateCollectionDialog` + `CreateItemDialog` that hides the trigger label below `sm` (icon + `aria-label`); only `TopBar` passes it, so the `/items/[type]` page header keeps its full "New Snippet" label. `TopBar` also hides the **wordmark** below `sm` (the DS mark still carries the link) and gained the missing flex sizing: **`shrink-0`** on the logo + actions, **`min-w-0`** on the search so it absorbs the squeeze. Header now fits (`scrollWidth` 390 = `clientWidth`); both dialogs verified opening. **3. Hero arrow pointed left, not down, on mobile** (`fix:` `7121b13`, `globals.css`) — a **double rotation**: `Hero.tsx`'s `max-md:rotate-90` emits **Tailwind v4's independent `rotate` property**, while the `mk-arrow-pulse-rot` keyframe *also* set `transform: rotate(90deg)`. Under v3 both would have collided on `transform` and the animation would have won at 90°; in **v4 `rotate` is its own property that composes with `transform`**, so 90° + 90° = **180°**. Note this corrects the 2026-07-16 homepage entry above, which recorded "arrow computed `rotate(90°)` (points down)" as verified — reading the computed **`transform` alone reports a reassuring `rotate(90°)` and hides the other half**; the bug only surfaces by measuring where the arrowhead actually lands relative to the circle's centre (or by reading `getComputedStyle(el).rotate`). Fixed by **deleting the redundant `@media (max-width: 767px)` block + its `mk-arrow-pulse-rot` keyframes** (−15 lines) — direction is now owned solely by the `rotate-90` utility and the single `mk-arrow-pulse` keyframe only scales, so it works at both breakpoints and reduced-motion (which kills the animation) still points correctly. Verified: mobile → DOWN with the preview below it, desktop → RIGHT with the preview to its right, pulse intact. **4. Marketing top bar on the auth pages** (`feat:` `a6d6b55`, `(auth)/layout.tsx` + `SiteNav.tsx` + `data.ts`) — started as "add the DevStash logo top-left linking home", then extended to the full homepage bar. `SiteNav` now renders on **all four** auth pages via their shared `(auth)/layout.tsx`, **replacing the centred DS mark** above the form (decisions confirmed with the user: move rather than duplicate; all four pages rather than just sign-in/register). Reuses the existing `Brand` (already links to `/` with an `aria-label`) rather than a new component. The nav sits in a **real header row with `pt-16`** clearing it, not absolutely positioned, so it can't cover the form on short viewports. `SiteNav` gained a **`usePathname` check that drops whichever CTA points at the current page** (no "Sign In" button on `/sign-in`); it was already a client component. **`NAV_LINKS` became root-relative** (`/#features`, `/#pricing`) — required, since bare hash anchors would dead-end on the auth pages, which have no such sections; on the homepage they still smooth-scroll without reloading (verified same-document). The two backgrounds happen to match almost exactly (`--color-mk-bg` `#0a0a0b` vs the app's dark `--background` ≈ `#0a0a0a`), so the translucent nav shows **no seam** — worth remembering if the app background ever changes. **Root-cause note:** fixes 1 and 2 are the *same* underlying trap — flex/grid children default to `min-width: auto` and won't shrink, so any long unbreakable child (code lines, `whitespace-nowrap` buttons) silently pushes the layout past the viewport; only the two reported rows were audited. **No DB schema/migration change.** No unit tests — all four are CSS/markup/wiring with no server-action or `src/lib` surface (the only tested scope); the existing 199 tests still pass. The unrelated pre-existing `.claude/**` working-tree edits (agent-memory + `list-components/SKILL.md`) were again left **out** of the commits. Browser-verified throughout via Playwright at 390px + 1280px (measuring real geometry rather than trusting computed styles, per fix 3). `npx tsc --noEmit`, `npm run lint`, `npm test` (199 pass), and `npm run build` all pass (`/`, `/sign-in`, `/register`, `/forgot-password`, `/reset-password` all stay `○ Static`). (Completed)
- **2026-07-17** — Mobile UI review fixes (branch `fix/ui-review-mobile`, commit `1270bd7`, merged as `ac99225`). Live UI review (Playwright, at 375/768/1280px, signed in as the demo user) of the homepage + dashboard, then fixed the findings. **Process finding first:** the running dev server was serving **stale compiled CSS** from before the 2026-07-17 `fix/mobile-layout-and-auth-nav` work — HMR had silently failed to drop the deleted `mk-arrow-pulse-rot` keyframe, so the hero arrow rendered **left** on mobile (the exact bug that entry says was fixed). The served stylesheet still contained both `mk-arrow-pulse-rot` **and** `mk-arrow-pulse`; the source on disk only has the latter. Confirmed it was purely stale server state (not a code regression), **restarted the dev server**, and the arrow then composed to a single `rotate: 90deg` (points down) as intended. Lesson recorded: after deleting a CSS keyframe, restart the dev server — don't trust HMR; and reading computed `transform`/`rotate` alone can hide a double-rotation (measure real geometry). **Three fixes committed** (5 source files): **(1)** `ItemActionBar.tsx` — the item drawer's 5-button action row (Favorite/Pin/Copy/Edit + right-aligned Delete via `ml-auto`) was a single non-wrapping flex row; at 375px the Delete button overflowed the drawer edge (measured x=359→395 in a 375px viewport, trash icon clipped, barely tappable). Hid the button **text labels below `sm`** (`<span className="max-sm:hidden">` — icon-only on mobile, `aria-label`/`title` retained) and added `flex-wrap` as a safety net; verified Delete now ends at x=319 (375 mobile), labels return and stay one row at 768, unchanged at 1280. **(2)** `data.ts` + `SiteFooter.tsx` — six dead `href="#"` footer links (Changelog, About, Blog, Contact, Privacy, Terms) removed rather than shipped broken; kept two columns of **real** destinations — **Product** (Features `#features`, Pricing `#pricing`) + **Account** (Sign In `/sign-in`, Get Started `/register`) — and switched the footer's inner `grid grid-cols-3` to `flex flex-wrap gap-x-16 gap-y-8` so it reads as intentional with fewer columns (a `data.ts` comment marks where to re-add sections once those pages exist; deliberately did **not** fabricate Privacy/Terms/About pages or ship 404s). **(3)** `SiteNav.tsx` — the shared marketing/auth nav hid its Features/Pricing links below `md` with **no mobile fallback**. Added a hamburger (Menu/X, `md:hidden`, `aria-expanded`) opening a dropdown with the nav links + Sign In; the ghost Sign In bar button now `hidden md:inline-flex` (moves into the menu on mobile) so the bar (DS + Get Started + toggle) doesn't overflow at 375px. Menu closes on link tap; the existing `pathname` guard still drops whichever CTA points at the current page (so `/sign-in` shows Sign In in neither the bar nor the menu). Header height moved from the `<header>` to the inner bar so the dropdown grows the header cleanly; opaque bg now also triggers while the menu is open. **Plus a contrast fix** surfaced during the re-review: measured `--color-mk-dim` (`#6b6b78`) at **3.56:1** on the dark marketing surfaces — **fails WCAG AA** (4.5:1) for the small muted text it drives (footer headings/copyright, hero chaos-field labels/captions); lightened it to `#8a8a99` (**5.5:1**, passes), verified live. `--color-mk-muted` was already fine (6.73:1). **Re-reviewed** all fixes across 375/768/1280 incl. the tablet label boundary and the shared `/sign-in` nav — no regressions, console clean throughout. No unit tests — all changes are React components + CSS/data with no server-action or `src/lib` surface (the tested scope); existing suite (199) still passes. The `ui-reviewer` subagent couldn't run the review itself (its Playwright MCP tools are **deferred** in-session and don't load for subagents, even after fixing an escaped-underscore `tools:` frontmatter typo), so the review was driven directly from the main session. The unrelated pre-existing `.claude/**` working-tree edits (agent-memory + `list-components/SKILL.md`) and the `ui-reviewer.md` tooling tweak were left **out** of the commit. `npx tsc --noEmit`, `npm run lint`, `npm test` (199 pass), and `npm run build` all pass. (Completed)
- **2026-07-18** — Language dropdown above content with live highlighting (branch `feature/language-dropdown`, commit `39c5985`, `feat:`). Replaced the free-text **Language** `Input` in the item forms with a **searchable combobox** and moved it **above** the Content field, so picking a language applies **live syntax highlighting as you type**. Because both the new-item modal (`CreateItemDialog`) and the drawer inline-edit (`ItemDrawer`) consume the shared **`ItemEditForm`** — and both already threaded `language` through its state — this was a single-component change plus two new files; `CreateItemDialog`/`ItemDrawer` needed **no** changes. **Single source of truth** `src/lib/languages.ts` (new): `LANGUAGE_OPTIONS` — 34 curated languages as `{ value, label }`, where `value` is a **canonical lowercase id** that `CodeEditor`'s existing `toMonacoLanguage()` resolves directly to a Monaco language id (so storing `typescript`/`bash`/`dockerfile` — the seed's values — round-trips cleanly), plus `languageLabelFor(value)` which returns the curated label, else the **raw value verbatim** (so a legacy/unusual stored language still displays and re-saves unchanged), else `""` for empty/null. **Combobox** `src/components/items/LanguageCombobox.tsx` (new, `"use client"`): shadcn **Popover** + **Command** (cmdk) — an outline trigger showing `languageLabelFor(value)` (or a muted "Select language"), a searchable list of the curated options with a check on the active one, and re-selecting the current language **clears it** back to none. **Design decision** (confirmed with the user at `start`): *curated + searchable*, with values outside the list still shown/round-tripped — chosen over a plain `Select` or a free-text "Other" escape hatch. **`ItemEditForm.tsx`**: swapped the language `<Input>` for `<LanguageCombobox>` and reordered so the Language `Field` (still gated to `LANGUAGE_TYPES = { snippet, command }`) renders **before** the Content `Field`; the content editor is the existing `CodeEditor`, which already re-highlights live from its controlled `language` prop, so no editor change was needed. Installed the shadcn **popover** primitive (`src/components/ui/popover.tsx`) — the only new dependency (combobox needs it). **No DB schema/migration change** (`language` stays free text on `Item.language`; the server action / persistence layer are untouched). **Unit tests** `src/lib/languages.test.ts` (6 — `languageLabelFor`: known-value label, case-insensitivity, whitespace-trim, unknown-value passthrough, empty/null/undefined → `""`, and an invariant that option values are unique + lowercase); suite **199 → 205**. **Browser-verified** end-to-end via Playwright (signed in as the demo user, both surfaces): *create modal* — Language sits above Content, searching "pyth" → Python, selecting it flips the editor header to "python" and Monaco tokenizes `def`/`return`/`1` as `keyword.python`/`number.python` (live highlighting proven via `monaco.editor.tokenize`); *drawer edit* — the existing `typescript` round-trips as **TypeScript**, changing it to **Go** updates both the header label and the Monaco model language to `go` live; zero console errors. The unrelated pre-existing `.claude/**` working-tree edits (agent-memory + `list-components/SKILL.md`) plus other untracked docs/research were left **out** of the commit. `npx tsc --noEmit`, `npm run lint`, `npm test` (205 pass), and `npm run build` all pass. (Completed)
- **2026-07-18** — AI auto-tagging with OpenAI `gpt-5-nano` (branch `feature/ai-auto-tagging`, spec in `context/features/ai-auto-tag-spec.md`, commit `f4f135a`, merge `6f84aa2`). The **first AI feature** — suggests 3–5 freeform tags for an item from its title + content, rendered as **accept/reject chips** near the tags input in **both** the create dialog and the drawer edit form (both share `ItemEditForm`, so one integration covers both). Also stands up the **OpenAI foundation** for the later AI features. Installed `openai` (v6) + `server-only`. **Client** `src/lib/ai/openai.ts`: lazy `OpenAI` singleton (`import "server-only"` hard guard, `maxRetries: 2`, `timeout: 30s`, mirrors the `r2.ts`/`rate-limit.ts` fail-safe pattern), the single `AI_MODEL = "gpt-5-nano"` constant, and `isAIConfigured()`; the API key is read only here, never returned/logged. **Pro gate** `src/lib/ai/pro.ts` → `requireProUser()` (the app's **first real server-side Pro gate**): `requireUserId()` + a `prisma.user.findUnique({ select: { isPro } })`, returning `{ userId } | { error }`. **Tagging core** `src/lib/ai/auto-tags.ts`: **CRITICAL — uses the Responses API** (`client.responses.create({ model, instructions, input, text: { format: { type: "json_object" } } })` → `response.output_text`), since `gpt-5-nano` returns **empty content** via Chat Completions and `zodResponseFormat` blows its length limit; plus pure, tested helpers — `truncateContent` (2000-char clip), `normalizeTags` (lowercase, strip leading `#`, trim, dedupe, cap 5), `parseTagsResponse` (tolerant JSON parse handling **both** `{"tags":[…]}` and bare `[…]`, `[]` on anything unparseable), and `buildTagUserPrompt`. **Rate limit** `src/lib/rate-limit.ts`: added `aiAutoTag: 20/hour` to `RATE_LIMITS`, keyed by **userId** (authenticated), fails open like the rest. **Server action** `src/actions/ai.ts` → `generateAutoTags(input)` in the `{ success, data?, error? }` shape: Zod validate (title required; content/language optional/coerced) → **Pro gate** → **rate limit** → `isAIConfigured` → model call; returns `{ tags: [] }` (still success) when the model has nothing, so the UI shows "No suggestions" rather than an error. **UI**: `src/components/ai/pro-context.tsx` (`ProProvider` + `useIsPro`, mounted in `AppShell` from the DB user's `isPro`) carries Pro status to deep client UI without prop-drilling; `src/components/items/TagSuggestions.tsx` (`"use client"`) is the Sparkles **ghost** "Suggest Tags" button + accept(check)/reject(X) chips — **hidden entirely for free users** (`useIsPro`), calls the action, filters out tags the item already has (case-insensitive), accept appends to the form's comma-separated `tags` (dedupe) and persists through the existing create/update path (**no new write**). Wired into `ItemEditForm`'s Tags field (added `getCurrentUser` → `SidebarUser.isPro`, threaded via `AppShell` → `ProProvider`). **Bug caught by browser verification (not unit tests):** the first live call failed — the Responses API **rejects `text.format: json_object` unless the word "json" appears in the input message** (the system `instructions` don't satisfy the check); fixed by appending a JSON instruction line to `buildTagUserPrompt`. **Dev-Pro decision** (per the overview's "all dev users are Pro"): flipped the seed's demo user to `isPro: true` **and** applied a non-destructive live `UPDATE` on the Neon **development** branch so the existing demo account can exercise AI without a re-seed (wipe). Kept lean per the spec — no streaming, no shared `guard.ts`/`limit.ts` abstraction (deferred to when the other three AI features land). **Unit tests** (+26, suite **205 → 231**): `src/lib/ai/auto-tags.test.ts` (truncate, normalize, parse both shapes, prompt building — openai.ts mocked so `server-only` doesn't throw in Vitest), `src/lib/ai/pro.test.ts` (Pro allowed / non-Pro blocked / missing user), `src/actions/ai.test.ts` (empty-title reject without gating, gate-fail, rate-limit-fail, not-configured, success passthrough, empty-tags-still-success, thrown→generic; rate limit keyed by the gate's userId). **Browser-verified end-to-end** via Playwright (signed in as the now-Pro demo user, real OpenAI call): create dialog + drawer edit both show the button; title *"useDebounce React hook"* returned `react`/`debounce`/`hook` (lowercase), **accept** adds to the tags input and removes the chip, **reject** dismisses without adding; zero console errors. Committed the feature code + the spec + `docs/ai-integration-plan.md` + `context/research/ai-integration-research.md`; the unrelated pre-existing `.claude/**` working-tree edits (agent-memory + `list-components/SKILL.md` + `ui-reviewer.md`) were left **out**. `.env` already carries `OPENAI_API_KEY` (the `.env*` template edit is skipped — gitignored). `npx tsc --noEmit`, `npm run lint`, `npm test` (231 pass), and `npm run build` all pass. (Completed)
