// Dashboard home — main content area.
// Phase 1: placeholder only — collections grid and pinned items arrive in later phases.
export default function DashboardPage() {
  return <h2 className="text-lg font-semibold">Main</h2>;
}
